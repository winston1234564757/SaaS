# ~~[TECH_DEBT]~~ → [СКАСОВАНО] C2C Settings UI

## Статус
✅ **СКАСОВАНО — UI існує у Growth Hub / Loyalty Drawer**

Дивись `PLANFICH/c2c-settings-ui-missing.md` для пояснення.

## Коротко

C2C налаштування знаходяться у `LoyaltyPage.tsx` рядки 406–500, 
доступні через `/dashboard/growth?drawer=loyalty`.

Помилка аудиту: grep по `settings/` замість `loyalty/` та `growth/`.
