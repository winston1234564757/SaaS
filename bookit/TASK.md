Act as an Elite Senior Full-stack Architect. 

Поточна задача: Ітерація 2. Налаштування Routing-флоу для тарифів, фікс завантаження фото та розширення схеми профілю.



КРИТИЧНІ БАГИ ТА ЗАВДАННЯ ВІД КОРИСТУВАЧА:

1. Лендинг -> Реєстрація -> Онбординг: Користувач обирає тариф на лендингу, але після авторизації система це забуває і кидає одразу на онбординг. Має бути: вибір тарифу -> авторизація -> сторінка оплати (якщо тариф платний) -> онбординг.

2. У профілі майстра відсутнє поле "Мобільний телефон" (Phone), хоча UI налаштувань його вимагає.

3. На онбордингу не зберігається фото майстра.



ВИКОНАЙ НАСТУПНИЙ ХІРУРГІЧНИЙ ПЛАН:



1. **Persist Pricing Intent (Cookies)**:

   - Модифікуй компоненти вибору тарифу на Landing page (`src/components/landing/LandingPricing.tsx` або подібні). При кліку на тариф записуй його ID у cookie (наприклад, `intended_plan`).

   - Онови Auth-флоу (перевір `src/app/auth/callback/route.ts` та `register/actions.ts`). 

   - Налаштуй динамічний редірект після успішної реєстрації/логіну: система має прочитати cookie `intended_plan`. Якщо це платний тариф — редірект на сторінку оплати/білінгу. Якщо успішна оплата або тариф free — редірект на `/onboarding`. Після обробки — очистити cookie.



2. **Database Schema & Phone Field**:

   - Перевір `src/types/database.ts` та SQL міграції. Додай поле `phone` (text/varchar) до таблиці майстрів/профілів. 

   - Додай валідацію та інпут мобільного телефону в UI `OnboardingWizard.tsx` та `SettingsPage.tsx`.



3. **Storage & Avatar Fix**:

   - Проведи дебаг `ImageUploader.tsx` у зв'язці з онбордингом.

   - Перевір, чи правильно генерується public URL після завантаження в Supabase Storage.

   - У Server Action, який зберігає цей URL у базу, ОБОВ'ЯЗКОВО додай `revalidatePath('/onboarding')` та переконайся, що клієнтська частина оновлює свій стан (invalidateQueries). 

   - Перевір RLS політики для бакета Storage (чи має authenticated user право на INSERT/UPDATE).



ОБМЕЖЕННЯ:

- Збереження тарифу реалізувати виключно через Cookies, щоб пережити OAuth-редиректи.

- Не зламай існуючу логіку сесій Supabase.

- Після імплементації надай стислий звіт українською мовою.