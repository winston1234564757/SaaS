Дій як Elite Senior Full-stack Architect. 

Ми розпочинаємо масштабний рефакторинг та багфікс SaaS-проєкту (Next.js 15 App Router, Supabase, TanStack Query v5).

Поточна задача: Ітерація 1. Хірургічне втручання в реактивність даних, виправлення Hydration mismatches та синхронізацію кешу.



КРИТИЧНІ БАГИ ВІД КОРИСТУВАЧА:

1. (ТОП ПРІОРИТЕТ) На сторінці Onboarding при першому рендері нічого не зберігається і висить безкінечний спінер завантаження.

2. На сторінці Settings (Налаштування) при першому рендері кнопки та редагування не працюють, збереження висить на вічному спінері, тултіпи не реагують. Після ручного оновлення сторінки (F5) — все працює ідеально.

3. На сторінці Settings (графік роботи/вихідні) по дефолту, якщо вихідних немає, безкінечно рендериться скелетон замість нормального порожнього стану.

4. Ця проблема стосуються ВСЬОГО ПРОЄКТУ, проаналізуй всі файли і виправ це раз і назавжди



ВИКОНАЙ НАСТУПНИЙ ЖОРСТКИЙ АРХІТЕКТУРНИЙ ПЛАН:



1. **Глобальний аудит React Query та Server Actions**:

   - Проаналізуй файли ...page.tsx та всі пов'язані з ними хуки (наприклад, для витягування профілю, графіку чи налаштувань).

   - Забезпеч ПОВНУ відсутність stale cache. Всі Server Actions, що мутують дані, ПОВИННІ викликати `revalidatePath` та/або `revalidateTag`.

   - Всі клієнтські `useMutation` ПОВИННІ у блоці `onSuccess` викликати `queryClient.invalidateQueries` для відповідних ключів. Ніхто не повинен натискати F5 для оновлення UI.



2. **Ліквідація вічних спінерів (Hydration & Suspense Fix)**:

   - Знайди причину блокування UI на першому рендері. Перевір, чи не конфліктують серверні компоненти з клієнтськими станами `isLoading`.

   - Використовуй `isPending` з TanStack Query v5 замість застарілих практик. 

   - Виправ тултіпи в налаштуваннях (ймовірно, проблема з ініціалізацією Radix UI / Shadcn через зламаний первинний рендер).



3. **Імплементація Empty States замість Скелетонів**:

   - У логіці рендеру вихідних/відпусток знайди місце, де повертається `[]` (пустий масив) або `null`. 

   - Прибери безкінечний рендер скелетона для цих випадків.

   - Додай UI-елемент для порожнього стану з текстом: "Запорука якісної роботи - якісний відпочинок, зроби собі вихідний)".



ОБМЕЖЕННЯ ТА ВИМОГИ:

- Жодних милиць типу `window.location.reload()`. Тільки нативна реактивність.

- Збережи строгу типізацію відповідно до `database.ts`.

- Після виконання фіксу, надай короткий звіт українською мовою про те, які саме файли були змінені і в чому була справжня причина (Root Cause). Не ламай існуючий UI. Також пропиши мені інструкція для тестування, що дивитись і що як має працювати.