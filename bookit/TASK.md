Act as an Elite Senior Full-stack Architect. 
Поточна задача: Ітерація 4. Реанімація модуля Studio (фікс ціни та логіки створення).

КРИТИЧНІ БАГИ ВІД КОРИСТУВАЧА:
1. На сторінці `/dashboard/studio` (або в компоненті пропозиції створення `StudioJoinPage.tsx`) залишилась стара ціна 199. Потрібно змінити на 299.
2. Зламана логіка створення: при натисканні "Створити студію" запит іде, студія в БД створюється, але на фронті нічого не відбувається. При повторному кліку вилітає помилка "Ви вже у студії", і користувач блокується на екрані створення, не маючи доступу до дашборду студії.

ВИКОНАЙ НАСТУПНИЙ ХІРУРГІЧНИЙ ПЛАН:

1. **Оновлення Прайсингу**:
   - Знайди хардкод ціни 199 у файлах, пов'язаних із `src/app/(master)/dashboard/studio/` (ймовірно `StudioJoinPage.tsx`, `StudioPage.tsx` або компоненти лендингу студії). Зміни на 299.

2. **Ремонт логіки створення (`src/app/(master)/dashboard/studio/actions.ts` та UI)**:
   - Проаналізуй Server Action, що відповідає за створення студії (наприклад, `createStudio`).
   - ПІСЛЯ успішного створення студії та прив'язки майстра, екшен ОБОВ'ЯЗКОВО повинен викликати `revalidatePath('/dashboard/studio', 'layout')` та `revalidatePath('/dashboard', 'layout')`, щоб скинути кеш поточного юзера.
   - У клієнтському компоненті, який викликає цей екшен (кнопка створення), переконайся, що після успішного виконання (`onSuccess` якщо це React Query, або перевірка результату екшена):
     а) Відбувається `queryClient.invalidateQueries({ queryKey: ['profile'] })` (або ключі, пов'язані з юзером/студією).
     б) Робиться примусовий рефреш роутера (`router.refresh()`) або динамічне перемикання стейту, щоб замість екрану "Створити студію" миттєво відрендерився дашборд студії.

3. **Захист від "Ghost State"**:
   - Перевір кореневий `page.tsx` для студії. Якщо у майстра вже є `studio_id`, він НІКОЛИ не повинен бачити екран створення/реєстрації студії. Його має пускати одразу всередину.

ОБМЕЖЕННЯ:
- Переконайся, що після виправлень не зламалася логіка запрошення (join) інших майстрів у студію, якщо вона там є.
- Жодних `window.location.reload()`. Робимо чисто через механізми Next.js та React Query.
- Надай короткий звіт українською мовою.