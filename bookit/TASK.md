«Ти — Головний Архітектор BookIt. Лендинг виглядає чудово, але ми маємо зробити бізнес-логіку залізобетонною. Виконай наступні кроки:

Server-Side Guard: В createBooking.ts додай перевірку: якщо майстер на тарифі starter і вже має >= 30 записів за поточний календарний місяць — кидай Error('LIMIT_EXCEEDED').

Studio Synergy: Створи роут src/app/studio/[slug]/page.tsx, який відображатиме профіль студії, список майстрів та дозволятиме обрати послугу будь-якого з них. Це ключова цінність тарифу Studio.

Smart Upsell: У AnalyticsPage.tsx та ClientsPage.tsx реалізуй 'Motivational Empty States'. Якщо масив даних порожній — замість графіків показуй красиву картку-інструкцію, як залучити перших клієнтів.

Financial Sync: Переконайся, що при створенні бронювання через createBooking.ts реально враховується flash_deal_id та loyalty_discount. Ціна в таблиці bookings має бути ФІНАЛЬНОЮ, а не базовою.

Typo Audit: Пройдися по src/lib/utils/dates.ts та переконайся, що всі виклики pluralize мають правильні закінчення для української мови.

Працюй автономно. Роби код так, ніби ти будуєш систему для мільйонів транзакцій.»**