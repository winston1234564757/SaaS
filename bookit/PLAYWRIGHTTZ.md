Ти — Lead QA Automation Architect. Наша мета: покрити E2E-тестами весь SaaS-проєкт "Bookit" (Next.js + Supabase) відповідно до файлу BOOKIT_TZ.md.



КРИТИЧНЕ ПРАВИЛО: Ми працюємо СУВОРО ПОЕТАПНО. Ти не маєш писати всі тести одразу. Після виконання кожної фази ти повинен написати звіт і чекати моєї команди "Продовжуй" для переходу до наступної.



АРХІТЕКТУРА ТЕСТІВ (Playwright):

Використовуй патерн Page Object Model (POM). Всі локатори та методи сторінок мають бути в `e2e/pages/`. Самі тести в `e2e/tests/`. Враховуй специфіку Next.js: використовуй `await expect().toBeVisible()` замість жорстких таймаутів, оскільки дані можуть вантажитись асинхронно.



ФАЗА 1: Фундамент та Smoke-тести

1. Ініціалізуй Playwright у проєкті. Налаштуй `playwright.config.ts` (базовий URL, браузери, репортер).

2. Створи структуру папок `e2e/pages` та `e2e/tests`.

3. Створи базовий POM для Landing Page (`e2e/pages/LandingPage.ts`).

4. Напиши `smoke.spec.ts`: перевірка, що головна сторінка `/` та `/explore` рендеряться без крашів (перевірка h1 заголовків).

5. Додай скрипти в package.json (`test:e2e`, `test:e2e:ui`).

[Після цього зупинись і чекай мого дозволу].



ФАЗА 2: Авторизація та Ізоляція Ролей (Security & Routing)

1. Створи `AuthPage.ts`.

2. Напиши `auth.spec.ts`.

3. Напиши тест логіну Майстра (імітація сесії або UI-логін) -> перевірка редіректу на `/dashboard`.

4. Напиши тест логіну Клієнта -> перевірка редіректу на `/my/bookings`.

5. Тест безпеки: спроба клієнта зайти на `/dashboard` має кидати його назад або на 403.

[Зупинись і чекай].



ФАЗА 3: Master CRUD (Бізнес-логіка майстра)

1. Створи `DashboardPage.ts` та `ServicesPage.ts`.

2. Напиши `master-crud.spec.ts`.

3. Тест: Створення нової послуги -> перевірка, що вона з'явилась у списку.

4. Тест: Створення нового товару.

[Зупинись і чекай].



ФАЗА 4: Booking Flow (Гібридний кошик - Кіллер-фіча)

1. Створи `PublicBookingPage.ts`.

2. Напиши `booking-flow.spec.ts`.

3. Симуляція шляху клієнта: Захід на `/[slug]` -> Вибір послуги -> Вибір часу -> Додавання товару -> Підтвердження запису.

4. Перевірка: чи правильно порахувалася загальна сума (послуга + товар) у Dynamic Island / Smart Cart.

[Зупинись і чекай].



Фаза 6. РЕПОРТИНГ ТА ЛОГИ: Жорстко налаштуй `playwright.config.ts` для збереження артефактів.

   - Додай генерацію HTML-репорту (reporter: [['html', { outputFolder: 'playwright-report' }], ['json', { outputFile: 'test-results/test-logs.json' }]]).

   - Увімкни збереження трейсів, скріншотів та відео ТІЛЬКИ при падінні тестів (trace: 'retain-on-failure', screenshot: 'only-on-failure', video: 'retain-on-failure').

   - Налаштуй відловлювання `page.on('pageerror')` та `page.on('console')`, щоб усі фронтенд-помилки з Next.js (наприклад, Hydration errors) записувались у логи тесту.

ЗАРАЗ: Прочитай BOOKIT_TZ.md, підтвердь, що зрозумів логіку проєкту, і виконай ТІЛЬКИ ФАЗУ 1.