import { LegalHubPage } from '@/components/master/documents/LegalHubPage';

export const metadata = {
  title: 'Документи — BookIT',
};

export default function DocumentsPage() {
  return <LegalHubPage />;
}
