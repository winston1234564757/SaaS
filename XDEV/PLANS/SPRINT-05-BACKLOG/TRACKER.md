# Sprint-05 TRACKER
> Живий статус. Оновлюється після кожної ітерації (⬜→✅).
> Деталі виконаного: `HANDOFF.md` | Повний план: `BACKLOG.md`

**Прогрес:** 74/87 ✅ · 3 ↩️ скасовано (M-DASH-11, M-MKT-01, M-MKT-02) | **Розпочато:** 2026-06-21 | **Оновлено:** 2026-07-03
> Total 84→86: +M-SHOP-05 (Нова Пошта, реальна NP API) +M-CLI-07 (CSV-експорт), додані під час M-BILL-02 як чесні борги (заявлялись у копі, але не реалізовані).
**🚀 ЗАДЕПЛОЄНО НА ПРОД 2026-07-01** (`vercel --prod`, останній deploy `dpl_5RnDcCrnSqmQAvGd6dRn39Ed9RNd` READY з M-BILL-01): усе до M-BILL-01 + ad-hoc фікси. Live=`bookit-winston1234564757s-projects.vercel.app` (bookit.com.ua НЕ прив'язаний). Git запушено (origin/main синхрон). ⏳ M-BILL-02 НЕ задеплоєно (чекає команди founder).
**▶ NEXT:** `С-CLI-01` — Клієнти: сторінка клієнта, повний редизайн (P1, Opus).
> ✅ **C-EXPL-01 (+C-EXPL-02) «Пошуковий портал» DONE (commit `373e9701`, НЕ задеплоєно):** /explore повний концепт з нуля (Закон темного блоку). Темний slate-портал (герой=пошук, Great Vibes-заголовок, категорії-типографіка однакового розміру з лічильниками) + sticky-компакт пошук при скролі (IntersectionObserver) + інтент-грід (герой з даних «Працюють сьогодні·N», чесний fallback today→tomorrow→top; чіпи Поруч/Завтра/PRO/Топ) + фільтри→vaul BottomSheet (ціна/сорт/відгуки, лічильник активних) + spotlight (1 топ-PRO з фото, дедуп, зникає без фото). C-EXPL-02 злито: MasterCard/MasterListCard/SpotlightCard з нуля — no-photo→монограм-обкладинка (детермінований Frost-hue) замість «сумної E», спеціалізація→темний пілл, чесні лейбли «Працює сьогодні/завтра» (не «слот» — `availableToday`=працює в цей DOW, НЕ вільний слот). 1290-рядковий моноліт розбито на `explore/*` (shared/cards/SearchPortal/IntentGrid/FilterSheet); усі хардкод-кольори→Frost-токени. Тип REDESIGN Тір 2. Скіли: brainstorming→impeccable(craft,delight)→grilling→design-taste-frontend+emilkowalski-motion→humanizer. Локальний рендер власними очима (Playwright headless iPhone13, мок /explore-preview, видалено) + founder на реальному девайсі (3 правки застосовані: Great Vibes-заголовок, категорії 1 розмір, темний пілл спеціалізації). TSC:0 Build:clean. Бриф: BRIEFS/C-EXPL-01.md.
> ✅ **C-NAV-01 «MyBottomNav FAB speed-dial» DONE (commit `ea1551b6`, НЕ задеплоєно):** 5 плоских рівних слотів → асиметрична ієрархія: центральний темний FAB-герой (#0F172A) + 4 тихі слоти (Закон темного блоку). /my: Записи·Каталог·[FAB]·Чат·Профіль; FAB→speed-dial springs-out (stagger, Plus→× rotate, scrim tap-to-close, close on route change) = **Бонуси·Сповіщення** (свап Каталог↔Бонуси за founder). Public authed: Записи·[FAB dial Каталог·Сповіщення]·Профіль; guest: Головна·[FAB=Каталог direct]·Увійти. Кошик→плаваюча піл над навбаром (не слот). NEW `NavSpeedDial.tsx` (ізольований memo-leaf, dial|direct mode). Motion=реюз TAP_POP spring{520/16/0.8} з QuickActions (одна мова), useReducedMotion gate, тільки transform/opacity. Реюз useUnreadDMCount/useActiveCart/NavLoginSheet/layoutId/isFullscreenChat. Сповіщення без live-badge (хука непрочитаних нема → DATA-борг). Тип MOTION Тір 1. Скіли: design-taste-frontend+emilkowalski-motion. Локальний рендер верифіковано (Playwright headless 390px, 2 стани, прев'ю видалено). TSC:0 Build:clean. Бриф: BRIEFS/C-NAV-01.md.
> ✅ **C-MSG-02 «Рейка "Написати майстру" + hairline під тайтлом» DONE (commit `a7ed44ce`, НЕ задеплоєно):** клієнтський список `/my/messages` — тиха горизонтальна рейка майстрів БЕЗ активної розмови (дедуп: `master.id`===`conversation.participant.id`; тап→`?to=id`→`getOrCreateConversation`→діалог). Реюз `ScrollStrip` (fade+стрілки+крапки+a11y). Спільний `getMyMasters()` (винесено inline-запит із `my/masters/page.tsx`) → одне джерело для /my/masters і рейки; UI MyMastersPage без змін. `MessagesListPage` master-safe (опційний проп `masters?`; майстер не передає → рейла нема). Hairline `border-b border-border/40` під хедером «Повідомлення». Лейбл «Написати майстру» (humanizer: відкинуто «Швидкий чат»/«Почати розмову»). Новий `MastersRail.tsx`. Тип MOTION+REDESIGN (Тір 1). Скіли: impeccable (craft) + scroll-experience (reuse ScrollStrip) + humanizer. a11y MCP: лейбл text-text-sub 6.79, імена text-foreground. Рендер власними очима (мок-прев'ю headless, видалено). TSC:0 Build:clean. Очікує деплою. Бриф: BRIEFS/C-MSG-02.md.
> ✅ **C-MSG-03 «Нова розмова: контакти по ролях» DONE (commit `e33faff2`, ad-hoc з репорту founder, НЕ задеплоєно):** кнопка «+» у хедері Повідомлень → адаптивний Sheet-пікер `NewConversationSheet` (role-aware). **Майстер:** клієнти з акаунтом (тап→діалог) + «Запросити в BookIT» без-акаунтним (share/Telegram по номеру). **Клієнт:** майстри з історії записів→діалог. **Адмін:** у `AdminSupportConsole` кнопка «Нова розмова» + таби Майстри/Клієнти→`createAdminTicketForUser`. Server (без нової DB): `getMessageableContacts`+`createAdminTicketForUser`+`getAdminMessageTargets` (admin role-guard). **🔴 Баги, зловлені founder-QA + звіркою по БД (supabase MCP):** (1) джерело клієнтів майстра було `client_master_relations` (5 рядків) → переписано на RPC `get_master_clients` (76 клієнтів, = сторінка Клієнти); (2) `master_profiles` PK = `id` НЕ `user_id` (колонки нема!) → `getMessageableContacts` клієнт-гілка + латентний `getOrCreateConversation` переведено на `profiles.role`+`master_profiles.id`; (3) self-записи майстра виключено; (4) роль через `profiles.role`; (5) **видалено дубль плаваючий inbox-fab** у майстра+клієнта (вхід: майстер hub-рядок+топбар, клієнт навбар+support-віджет; повний клієнт-mobile→C-NAV-01). Скіли: design-taste-frontend+humanizer. TSC:0 Build:clean. Founder перевірив. Очікує деплою.
> ✅ **Ретроспектива M-HELP-02 доповнення:** плаваючий клієнт-mobile fab з 02b видалено в C-MSG-03 (був інтерим, founder «видаляй»). 🔴 `C-MSG-01` уже ЗАКРИТО через M-CHAT-01 (redesign списку/чату + h-dvh keyboard fix — дослівно спека C-MSG-01). `C-NAV-01` (MyBottomNav клієнта з «Чат») зробить справжню клієнт-нижню-навігацію — мій плаваючий клієнт-мобільний InboxNavButton у M-HELP-02b = ІНТЕРИМ до C-NAV-01, там перенести в bottom-nav. Далі — клієнт-зона (B): C-EXPL/C-BOOK/C-PROF/C-MAST/C-NAV/C-PHONE/C-DESK.
> ✅ **M-HELP-02 «Підтримка: комунікація + інбокс у навігації» DONE (commit `aa7944f0`, НЕ задеплоєно):** дві частини. **02a (комунікація з підтримкою, без DB):** `SupportPage` — картка активної розмови (статус Відкрито/Вирішено + бейдж «Нова відповідь» коли остання репліка від команди [похідна `sender_id`, без read-таблиці] + «Продовжити розмову»); інакше дефолтна картка чат/Telegram. `SupportChatPage` — статус-чіп у хедері + presence-крапка (зелена-пульс/сіра) + живі години. `supportHours.ts` — **щодня 8:00-20:00** (підтв. founder) → «Онлайн · зазвичай відповідаємо за 15 хвилин» / «Офлайн · працюємо щодня, 8:00-20:00». **02b (єдиний інбокс DM+Support + кнопка «Чат» усіх ролей):** `getInboxSummary`+`getSupportChatState` (server, бейдж = DM-непрочитане `conversations` + відповідь підтримки, БЕЗ нової DB); `InboxNavButton` (спільний, 3 варіанти icon/fab/row, live-бейдж realtime `direct_messages`/`support_messages` + focus, унікальний канал `useId`); `MessagesListPage` — закріплений рядок «Підтримка BookIT» над DM; nav: DashboardTopBar+MobileHub (fab над дзвоником + рядок «Спілкування» у hub) майстер, PublicNavbar (десктоп)+my/layout (плаваючий fab, mobile) клієнт. Скіли: impeccable + humanizer. a11y #92400E/#0D6B2F (5.5+). TSC:0 Build:clean. Founder дав години. Очікує деплою.
> ✅ **M-CHAT-01 «Спільний месенджер-модуль + фікс iOS-клавіатури/хедера» DONE (commit `b15829e0`, НЕ задеплоєно, ad-hoc поза беклогом → total 86→87):** founder-репорт зі скріна — клавіатура лишала діру над інпутом, хедер залазив під статус-бар. **Root cause:** iOS Safari не стискає `100dvh`/`h-dvh` під клавіатуру (лише скролить сторінку). **Fix — спільні примітиви `src/components/shared/chat/`:** `useChatViewport` (висота=`visualViewport.height` на resize+scroll, `scrollTo(0,0)` проти iOS-зсуву, lock body overflow/overscroll); `ChatShell` (корінь `position:fixed inset-x-0 top-0 z-40` + height від хука — навмисно fixed: імунітет до скролу сторінки Й padding батьків-layout, тож safe-area володіє ЛИШЕ ChatHeader, без подвійного відступу); `ChatHeader` (закріплений, `pt-[env(safe-area-inset-top)]`); `ChatMessageList` (групування + роздільники днів через `src/lib/utils/chatGrouping.ts` — formatDayLabel Сьогодні/Вчора/дата, startsGroup/endsGroup 5-хв вікно; хвостик+час лише на кінці групи; ResizeObserver→скрол донизу при відкритті клави; showReadReceipts/emptyState props); `ChatComposer` (attach + auto-grow textarea Enter=send + send, safe-area-bottom). **Консумери:** `DirectChatPage` (DM клієнт+майстер) + `SupportChatPage` (support клієнт+майстер) = повний ChatShell (стали тонкими); `AdminSupportConsole` (десктоп-пульт) переюзає ЛИШЕ `ChatMessageList` (currentUserId=adminId з `admin/support/page.tsx`), лишає каркас черги+resolve. **Layout bypass** `my/layout.tsx`+`DashboardLayout.tsx` `isChatRoute` розширено на `/messages/[id]` (regex) — DM більше не в padded max-w-lg+навбар+FAB. Полиш: ConversationRow unread-контраст, touch-таргети 44px. Скіл: impeccable. **Device-confirmed на iPhone (майстер↔підтримка «вогонь»).** TSC:0 Build:clean. Очікує деплою. **Нюанс:** viewport має `interactiveWidget:'resizes-content'` — браузер теж ресайзить, движок поверх; якщо composer зсунеться — це ручка тюнінгу.
> ✅ **M-HELP-01 «Підтримка/Академія дедуп + навігація» DONE (commit `b475f465`, НЕ задеплоєно):** BACKLOG-напрям = «пріоритет Академії — перенести все». Обсяг за QA founder: FAQ → **3-й таб «Питання»** в Академії; Support → тільки комунікація + лінк. **Академія (`AcademyPage.tsx`):** таб Функції/Цілі/Питання; усі 7 FAQ-категорій (~30 Q&A) перенесено дослівно з SupportPage → `FAQ_CATEGORIES` + `FaqSectionGroup`/`FaqItem` (дзеркалять SectionGroup/ArticleItem: тіло=відповідь без кроків/CTA) — реюз дизайн-системи, нуль нового візуалу; `FaqChatNudge` під FAQ → чат. **Support (`SupportPage.tsx`, 310→77 рядків):** FAQ+топ-світчер прибрано; лишились чат/Telegram-CTA + картка «База знань → Академія». Крос-лінки в обидва боки. Обидві вже в навбарі (MobileHub/DashboardTopBar) — меню не чіпано. Тип REDESIGN/IA консолідація (Tier 2), 2 файли. Скіли: design-taste-frontend (reuse) + humanizer (новий копі). a11y text-text-sub. TSC:0. Founder QA пройдено. Очікує деплою. (M-HELP-02 = глибша комунікація на вже-зведеному Support.)
> ✅ **M-DOC-01 «Документи quieter + distill» DONE (commit `7fc7fffb`, НЕ задеплоєно):** `LegalHubPage.tsx` (/dashboard/documents). Обсяг за QA founder = **тихі картки 2×2** (зберегти бенто, не список). «Гучне» = 4 різні хью-акценти на утилітарній юр-сторінці + 3-рядкові описи + тоновані bg. **Quieter:** прибрано 4 легасі-хекси (#789A99/#D4935A/#5C9E7A/#6B5750) + `bg`-тінти → єдиний Frost-нейтрал (іконка-чіп `bg-background border-border`, іконка `text-foreground`). **Distill:** описи 3 рядки → 1 тихий рядок кожен; прибрано дубль-CTA «Відкрити →» (стрілка ↗ вгорі + клікабельна картка = достатній афорданс). Картки рівні 2×2 (`h-full min-h-[120px]`). Заголовок + контакт-картка лишені (тихі). a11y: усі дрібні тексти `text-muted-foreground/xx` → `text-text-sub`. Скіл: impeccable-design-polish (quieter+distill). 1 файл. TSC:0. Founder QA пройдено («все ганяв»). Очікує деплою.
> ✅ **M-ORD-01 «Сортування замовлень» DONE (commit `4752ea10`, НЕ задеплоєно):** замовлення майстра живуть у `ProductsPage.tsx` вкладка «Замовлення» (НЕ окремий роут; `useOrders` UnifiedSale shop+booking). Додано **select сортування** праворуч від смуги фільтр-пігулок (вже був фільтр за статусом): «Спочатку нові» (дефолт = поточна created_at desc) / «Спочатку давні» / «Більша сума» / «Менша сума». Клієнтський сорт `sortedOrders = [...orders].sort` (created_at localeCompare для часу — ISO string хронологічний, total_kopecks різниця для суми), працює РАЗОМ зі status-фільтром. `ScrollStrip` пігулок → flex-1 min-w-0, select shrink-0 (моб: пігулки скролять, select фіксований). Бекенд/`useOrders`/OrderCard НЕ чіпано. Тип REDESIGN/interaction, 1 файл. Скіл: senior-frontend (вже завантажений). humanizer: опції копі. a11y: chevron/select text-text-sub/foreground + aria-label «Сортування замовлень». TSC:0. Founder QA пройдено. Очікує деплою.
> ✅ **M-PORT-02 «Відгуки з пагінацією» DONE (commit `73a697a6`, НЕ задеплоєно):** обсяг за QA founder = **редактор майстра** (не публічний показ; там прив'язаних відгуків одиниці + легасі-персик клієнт-зони). Секція вибору відгуків у `PortfolioItemPage.tsx` (прив'язка відгуків до роботи): `max-h-48 overflow-y-auto` scroll по ВСІХ ~100 відгуках → **пошук** (по імені клієнта + тексту відгуку, case-insensitive) + **пагінація** 5/стор (Назад/Далі `ChevronLeft/Right` + лічильник «1-5 з N») + **«Обрано: N»** (вибір персистить крізь сторінки/фільтр, `selectedReviewIds` окремий масив) + empty-результат «Нічого не знайшлося». Пошук скидає сторінку на 1; `safePage` клемпиться (не застрягне на неіснуючій сторінці після фільтра). **Бекенд НЕ чіпано** — `getMasterReviews` уже віддає до 100 відгуків, пагінація чисто клієнтська над пропом. Тип: REDESIGN/interaction, лише фронт, 1 файл. Скіли: senior-frontend + humanizer (плейсхолдер/лічильник/empty). a11y: нові дрібні тексти `text-text-sub`. TSC:0. Founder QA пройдено. Очікує деплою.
> ✅ **M-PORT-01 «Стандартизувати розмір карток» DONE (commit `e724e5ef`, НЕ задеплоєно):** обсяг за QA founder = вирівняти висоти + косметичний полиш (НЕ повний редизайн; розкладка лишається рівномірною галереєю cols-2/3/4 — закон темного блоку сюди свідомо НЕ тягнули, фото-галерея = легітимна рівномірність). **Root cause рваних висот:** фіксована обкладинка `aspect-[4/3]`, але змінний інфо-блок під нею (заголовок 1-2 рядки + 0-3 чіпи) + картки не `h-full` → CSS Grid не розтягував. **Fix:** ланцюг `h-full flex-col` (картка → кнопка flex-1 → інфо flex-1, чіпи `mt-auto` = рівна нижня лінія) + обгортка-`motion.div`/add-card `h-full` (той самий патерн, що MasterCard `/explore` uniform-height). Сторіс-оверлей переприв'язано до обкладинки (обгортка `aspect-[4/3]` замість крихкого `bottom-[52px]`, що дрейфував при stretch). **Токени:** усі легасі-хекси (`#C8B8B2`/`#E8D5CF`/`#EBD5CC`/білі rgba/персиковий `rgba(212,147,90)`) → Frost `bg-secondary`/`border-border`/семантичні. **a11y (MCP):** `text-muted-foreground`→`text-text-sub` (5.98 на surface), хінт `/50`→text-text-sub, upsell-лінк `text-warning` 4.34 провал → `#92400E` 5.71. Скіл: impeccable-design-polish (polish+colorize). 2 файли. TSC:0. Build батчиться перед деплоєм. Founder QA пройдено. Очікує деплою.
> ✅ **M-BILL-02 «Чесний перелік фіч + Pro домінантний» DONE (commit `b668dd55`, НЕ задеплоєно):** задача переросла з COPY у REDESIGN+аудит. **Головне: аудит tier-гейтів проти обману клієнта** — простежено enforcement по ~13 фічах, виправлено 5+ хибних Pro-заявок. Доступне Starter (перевірено кодом, НЕ Pro-ексклюзив): кілька послуг за візит (`createBooking`), лояльність (без гейта), розумні ціни (тріал до 1000₴, `createBooking:270`), пряма переписка (T-chat), QR (`SharePageCard`). Реальні Pro-гейти: безліміт записів (кап 40 `createBooking:179`), без вотермарки (`PublicMasterPage:1077`), розширена аналітика (`AnalyticsPage:111`), магазин на сторінці (`:792`), кастомна тема (`TechnicalIsland:49`), сторіс без розмиття (`useStoryEditor:157`). **Прибрано фейки:** Нова Пошта (не реалізовано→M-SHOP-05), CSV (enforcement не знайдено→M-CLI-07). **Єдине джерело** `lib/constants/tierFeatures.ts` (Starter/Pro/Studio) → BillingPage+LandingPricing, кінець розбіжності 9-vs-13. **Billing:** рядки Starter/Studio розкривні (accordion aria-expanded, `FEATURES_BY_TIER`, Studio «Хочу в бету» sibling-кнопка). **Landing:** Pro домінантний ДИЗАЙНОМ (бейдж «Рекомендуємо» + `-mt-6 scale-[1.03] z-10`, бо чесних 2x-фіч немає — Pro=«Starter без лімітів»); теплий підрядок «Коли ти ростеш, Pro росте з тобою.»; Studio ожила («Формуємо першу групу студій» green + клікабельний пункт беты «Ваші ідеї та потреби формують продукт →» окремий фрейм від CTA); «Мультипослугові»→«Кілька послуг за один візит»; сторіс як унікальна фіча «Сторіс для Instagram із вільних вікон»; футер em dash прибрано. **Скіли:** multi-perspective-analysis (Studio-бета + Starter/Pro бізнес-аналіз: Pro продають ЛІМІТИ не фічі, апсел у точці болю не на /billing) + humanizer (весь копі) + design-taste-frontend. a11y: text-text-sub (5.94), green/slate токени. Рендер власними очима: landing desktop+mobile (3 ітерації). Billing accordion піксельно НЕ перевірено (за auth) — текст = те саме джерело. TSC:0 Build:clean.
> ✅ **M-BILL-01 «Тариф + бренд Monobank» DONE (commit `441e1e1f`, deploy `dpl_5RnDcCrnSqmQAvGd6dRn39Ed9RNd` READY):** повний редизайн `BillingPage.tsx` за Законом темного блоку. **State-aware герой:** Starter→офер Pro (serif-ціна 700₴, УСІ 9 можливостей, домінантна біла CTA), Pro→керування підпискою (3 стани: active «Наступне списання X» / canceled «Діє до X, автопродовження вимкнено» / no-card «Прив'язати картку»; чіп статусу картки за токеном — дедуп із рядком). **Monobank-панель** (col-4) замість фейк-тумблера з емодзі-лимоном: **офіційний monobank SVG-лого** (`public/monobank-logo.svg`, Wikimedia, інверт `brightness(0) invert(1)` у біле) на плаксому чорному #0A0A0A + інфо-довіра (SSL/картка в Monobank/керуй будь-коли) + тиха бренд-мітка картки. Плани = **1 герой + 2 тихі диференційовані рядки** (Starter «Ваш план» + Studio «Скоро»→бета), НЕ 3 однакові картки. Два темні блоки диференційовано матеріалом (slate editorial+аврора+Cormorant vs плаский чорний sans «чіп») — §5 закону. Бекенд/actions (`createMonoInvoice`/`cancelSubscription`/`recoverCardToken`/`submitBetaRequest`) НЕ чіпано. Мертвий `provider`-стейт видалено. a11y MCP (trust white/50=5.23, emerald-chip=11.71), humanizer (em dash прибрано), рендер власними очима 8 скрінів (4 стани × desk/mob) через мок-прев'ю-роут (видалено). Ритуал: brainstorming→impeccable craft→self-grill→бриф→humanizer. Founder дав напрям (усі 9 фіч + офіційний SVG) в 2 ітерації. TSC:0 Build:clean.
> ✅ **M-SET-04 «Відпустки + вихідні» DONE (commit `207f1955`):** редизайн секції відсутності — концепт «Коли мене немає», широка секція власним рядом (col-10). **Об'єднано з тижневими днями** (рішення founder «чистіше»): тижнева банда-герой Пн–Нд (тап = робочий/вихідний, пише `schedule.is_working`) + hero «N/7 робочих днів» + разові відсутності (відпустка/вихідний/короткий день: список + форма). 🔴 Керування «які дні» ПОВНІСТЮ переїхало сюди — з ScheduleWidget прибрано тумблер дня (лишились години/буфер/перерви + натяк). Єдиний стан `useSettingsForm` → без конфлікту. Split `VacationManager`(контейнер+useTimeOff+onScheduleChange) / `VacationManagerView`(презентація props) → рендер власними очима. Грід SettingsPage перегруповано: Retention col-4 + Segments col-6 + Vacations col-10. a11y: muted-foreground/xx → text-text-sub. Верифіковано власними очима (desktop+mobile, повний+empty). TSC:0 Build:clean. Founder QA пройдено. Очікує деплою.
> ✅ **M-SET-05 «Логіка заповненості по днях + смарт-статус» DONE (commit `c8b4d9ff`, зроблено поза чергою на репорт founder):** BUGFIX ScheduleWidget. **Баг:** о 8:50 (день ще не почався) статус казав «Зараз перерва». **Root cause:** `isWorkingNow` бінарний (в робочих годинах / ні) — усе інше (до відкриття / після закриття / вихідний) валилось у «Зараз перерва»; реальні `breaks[]` не перевірялись; `now` рахувався раз при рендері без тику → статус застрягав до reload. **Fix:** 5 станів (working/break/before «Відкриття о HH:mm»/after «Робочий день завершено»/off «Сьогодні вихідний»), `breaks[]` тепер детектять реальну перерву, хвилинний тик (`setInterval` 30с) оновлює на межах, спільні `getNow()`/`toMins()`. a11y лейбли #0D6B2F (5.16)/#92400E (5.56)/text-text-sub (5.94). Заповненість по днях звірено — коректна (forward 30д, 0% бо нема записів, не баг). Верифіковано власними очима всі 5 станів через time-travel cookie (`next-public-debug-now`). TSC:0 Build:clean. Founder QA пройдено. Очікує деплою.
> ✅ **M-SET-03 «BookIT Assistant активний + лінки» DONE (commit `721d9182`):** SmartAdvisor редизайн — концепт «Наступний крок». Пасивні поради → **клікабельні**: тап будь-якої → `scrollIntoView` до секції (#hero/#identity/#technical/#categories) + підсвітка `.advisor-highlight` (keyframe ring-флеш 1.7с, reduced-motion aware). Візуал: **банний hero-metric (7xl % score) → демотовано** в чесний «N / 4» + слім-смуга; **герой = найважливіша наступна дія** + афорданс «Перейти до розділу ›»; решта = компактні клікабельні рядки з chevron. 🔴 **ФЕЙК прибрано:** пояснення стверджувало «% заповненості → позиція в каталозі» — звірка `explore/page.tsx` показала сорт за `rating_count` (відгуки), НЕ заповненістю → переписано чесно (заповненість → довіра/конверсія; ранжування = відгуки/записи). a11y: дрібний `text-text-mute` (2.76) → `text-text-sub` (5.94 a11y MCP). SettingsPage +`id="categories"`. Тип виявився REDESIGN+interaction (не COPY) — звірка живого коду. TSC:0 Build:clean. Верифіковано власними очима (empty/partial/done через devpreview+Playwright). Founder QA пройдено. Очікує деплою за командою.
> ✅ **M-SET-02 «Дизайн блоку інфо профілю» DONE (commit `ded4332d`):** ProfileHero редизайн — концепт «Опублікована обкладинка». Плаский нижній стос (sans-ім'я + bio-рамка + 2 дрібні конкуруючі дії) → **асиметрія**: фото-герой на всю картку + editorial-скрім + **ім'я Cormorant serif = єдина домінанта** + тихий підвал (рейтинг) + **одна домінантна дія** full-width «Переглянути сторінку» (копіювати = вторинний icon). 🔴 Serif-ім'я на світлому фото втрачало контраст → скрім `from-black/85` + `text-shadow` (верифіковано на екстремально яскравому фото через прев'ю-рендер). Empty-стан вимитий → темний slate-placeholder (камера+нудж+serif читаються, «обкладинка в очікуванні фото»). a11y: `#D4935A`→`text-warning` (#B45309), `muted-foreground/60`→`text-text-sub` (5.88). Bio прибрано (живе в Identity), −1 prop у SettingsPage. Identity-дубль лишено (рішення founder). TSC:0 Build:clean. Founder QA пройдено («це воно»). Очікує деплою за командою.
> ✅ **M-ANL-07 «Зростання» DONE (commit `987b3d67`) — Analytics ПОВНІСТЮ закрита (7/7):** таб `growth` editorial-redesign — концепт «двигуни росту». 8 рівних карток (BentoSecondary 6 + GrowthLists + cohort) → асиметрія: **ТЕМНИЙ герой** (alternating: …Поведінка світлий · Зростання темний) = додатковий дохід від розумних цін (+₴N домінанта `metric-value` + врятовані слоти) + by-numbers (топ-20% частка / флеш заброньовано / розсилки→записи); **fallback при uplift=0** → LTV-концентрація + нудж «Увімкнути розумні ціни»→`/dashboard/revenue`. **LTV Парето featured** (col-7): концентрація% + гістограма СТАТИЧНИХ смуг + winback герой+ранг клік→профіль. **Канали залучення** (col-5): флеш/розсилки/реферали/постійні клієнти диференційованими рядками. **Спрацювання розумних цін** (col-5) + **Що бронюють разом** (col-7) смугами. **Когорти** (col-12, reuse `CohortHeatmap`). **🔴 Чесні лейбли:** «Постійні клієнти» + «N візитів сумарно» замість вигаданих «Клуб лояльності / нараховано балів» (хук рахує `total_visits`, не бали). **🔴 Усі смуги СТАТИЧНОЇ ширини** — `LtvHistogram`/`ServicePairingMatrix` мали framer animate-width (зникає в headless) → переписано в GrowthTabView. Консолідовано у `GrowthTab`(props)+`GrowthTabView`; **видалено 8 мертвих файлів** (BentoSecondary, GrowthLists, DynamicPricingUplift, FlashDealsCard, BroadcastEngagement, LtvConcentration, ServicePairing, LtvHistogram). GoalProgress прибрано зі Зростання (founder); ServicePairingMatrix лишено (тип). Pro-gate value-екран. a11y #92400E (5.56). Верифіковано власними очима (full/noUplift/mobile). TSC:0 Build:clean humanizer✓. Очікує візуального QA founder.
> ✅ **M-ANL-06 «Поведінка» DONE (commit `f21d84fd`):** таб `behavior` editorial-redesign — концепт «ритм бізнесу». 4 рівні стопки-блоки (heatmap-картка + LeadTime + NoShow + Vacation) → асиметрія: **СВІТЛИЙ герой** (alternating: …Фінанси темний · Поведінка світлий) = теплова карта завантаження ВЕЛИКА (col-7, не втиснута) + **пік-інсайт домінантою** (найзавантаженіший день serif + % о годині, клік→пояснення формули). **No-Show = герой секції** (col-7): рейти неявок/скасувань + журнал інцидентів клік→`OverviewDetailSheet`→профіль клієнта (`/dashboard/clients?clientPhone`). **Lead time** (col-5): середній час serif + розподіл смугами СТАТИЧНОЇ ширини (НЕ framer animate — зникає в headless, урок M-ANL-05). **Vacation** (col-12 quiet): чесний аналіз втрати + реальний лінк. **🔴 ФІКС ФЕЙКУ:** прибрано VacationTab «Рятувальник відпустки» цілком — кнопка «Оптимізувати розклад» (лише тост) + «Врятуйте ₴X» (`lost*0.4` вигадка) ВИДАЛЕНІ; лишився чесний аналіз (сер.день × дні вихідних, реальні дані) + реальний лінк «Налаштувати вихідні»→`/dashboard/settings#vacations`. **Консолідовано** 3 під-таби (NoShowTab/LeadTimeTab/VacationTab) + інлайн heatmap → один `BehaviorTab`(fetch)+`BehaviorTabView`(презентація); старі 3 файли видалено. Pro-gate value-екран (як Finances/Stock). a11y: `text-muted-foreground/60`→`text-text-sub`, #92400E (5.56) дрібні warning, #B91C1C (5.08). Верифіковано власними очима (full/clean/mobile). TSC:0 Build:clean humanizer✓. Очікує візуального QA founder.
> ✅ **M-ANL-05 «Фінанси» DONE (commit `bbd45815`):** таб `finances` editorial-redesign — концепт «P&L звіт». 5 рівних KPI-карток + WaterfallChart → асиметрія: **ТЕМНИЙ герой** (alternating: …Склад світлий · Фінанси темний) = чистий прибуток домінантою (`metric-value` tabular + ₴ окремо) + **Δ до минулого періоду** (Pro, 2-й виклик `useAnalyticsExtras` за `previousWindow`) + by-numbers виручка/витрати/чиста маржа; loss-стан rose-300. **Чесний каскад руху грошей** (світла секція col-7): реальний потік виручка→мінус собівартість/знижки/опекс→прибуток, hairline-смуги СТАТИЧНОЇ ширини (не framer width — зникає в headless), нульові рядки відфільтровано. **Маржинальність** (col-5): герой ризику = найтонша маржа (<40% warning / інакше neutral) + ранг-список; клік→розбивка маржі `OverviewDetailSheet`; рекомендацію «+10%» прибрано → нейтральний лінк «Переглянути ціну». **🔴 Прибрано 4-й фейк (НЕ був у брифі):** `WaterfallChart` вигадував `noShowLoss = servicesRevenue*0.05` і роздував фейковий «Загальний вал» — файл видалено (єдиний споживач). Empty-стан Pro→skeleton без цифр (прибрано dummy `displayFin`). Розділено `FinancesTab`(fetch+Δ)+`FinancesTabView`(презентація). a11y: #0D6B2F (5.21), #B91C1C (5.08), emerald-300/rose-300 на slate. Верифіковано власними очима (profit/healthy/loss/mobile). TSC:0 Build:clean humanizer✓. Очікує візуального QA founder.
> ✅ **M-ANL-04 «Склад» DONE (commit `924105d6`):** таб `stock` editorial-redesign — концепт «склад під контролем». Рівні картки → асиметрія: **СВІТЛИЙ герой** (alternating: Огляд темний · Джерела світлий · Відгуки темний · Склад світлий) = найтерміновіший розхідник, днів до закінчення великим числом (`metric-value` а НЕ `heading-serif` — Cormorant рендерив "18" як "I8" oldstyle-цифрами) + світлофор-tone (critical ≤3 / warning ≤7 / normal) + by-numbers запас/витрата-30д/прогноз; клік→`OverviewDetailSheet`. Col-5: світлофор Критичні/Увага/У нормі + **список покупок** (count до закупівлі + Сформувати/Копіювати з copy-to-clipboard). Col-12: усі розхідники ранг-списком за терміновістю + status-dots. **Empty-state Pro (нема даних):** skeleton-силует (сірі смуги, БЕЗ цифр) + teaser — **прибрано фейкові dummy-products під блюром** (один з 3 фейків з §0 брифу). Розділено `StockTab` (fetch+стани) + `StockTabView` (презентація). **Латентний баг M-ANL-02 виправлено:** SourceTab DeltaChip-down `text-error`→`text-destructive` (`--color-error` не існує в Tailwind v4, мапінг `--color-destructive: var(--error)`). a11y: critical #B91C1C, warning eyebrow #92400E (5.56), normal eyebrow #0D6B2F (5.21) на periwinkle. Верифіковано власними очима (crit/ok/mobile). TSC:0 Build:clean humanizer✓. Очікує візуального QA founder.
> ✅ **M-ANL-03 «Відгуки» DONE (commit `ccebf398`):** таб `reviews` editorial-redesign — концепт «Репутація». 3 рівні картки (NPS/середній/розподіл) → асиметрія: **ТЕМНИЙ герой-cover** (alternating: Огляд темний · Джерела світлий · Відгуки темний) = середній бал serif-домінанта + зірки + Δ · **NPS спів-герой** (клік→пояснення composition) + Δ · by-numbers композиція промоутери(5)/нейтральні(4)/критики(1-3); **розподіл оцінок** клік-бакет→`OverviewDetailSheet` інсайт (статична гістограма, не фільтр — аналітика=інсайт, не дублює фільтр ReviewsPage); **стрічка останніх** клік→деталі read-only. Розділено `ReviewsTab` (fetch) + `ReviewsTabView` (презентація). `useReviewsMetrics` +2-й запит попереднього періоду → Δ середнього балу + Δ NPS (всім, не Pro-gated). Клік відгуку → `OverviewDetailSheet` read-only (НЕ `ReviewDetailSheet` — аналітика без id/publish, керування публікацією на ReviewsPage). a11y: Δ emerald-300/rose-300 на slate (11.71/9.44), hero hint white/40→white/55 (3.98 провал→6.09). Верифіковано власними очима (desktop/mobile). TSC:0 Build:clean humanizer✓. Очікує візуального QA founder.
> ✅ **M-ANL-02 «Джерела» DONE (commit `100b94b2`):** таб `source` editorial-redesign — концепт «Канали». Самотній donut → асиметрія: **світлий featured-герой** (головне джерело: serif-назва каналу + домінантна частка % + Δ + ₴ + by-numbers стрічка записи/завершеність/виручка + гліф каналу-якір у мертвому просторі) + **donut support** (a11y-фікс: `muted-foreground/60`→`text-text-sub`, Frost-палітра slate/indigo/emerald/amber/muted) + **ранг-список «Інші канали»** (#2-N: смуга-частка відносно лідера, Δ-тренд, виручка, клік→`OverviewDetailSheet`: записи/завершено/завершеність/середній чек). **Герой світлий бо Огляд темний — alternating темний герой через таб** (рішення founder, проти втоми від темного). Розділено `SourceTab` (fetch+стани) + `SourceTabView` (презентація, для прев'ю/тестів). `useSourceAttribution` адитивно: +2-й запит попереднього періоду → per-канал Δ% (Pro), +`completedCount`→завершеність, revenue копійки→гривні (консистентно з formatPrice). **Верифіковано власними очима** (Playwright headless: desktop/mobile/single-channel). a11y: Δ-up `#0D6B2F` (5.21) замість `text-success` #16803C (3.93 провал малого тексту на periwinkle). TSC:0 Build:clean humanizer✓. Очікує візуального QA founder.
> ✅ **M-ANL-01 «Огляд» DONE (commit `2c48474f`, ЗАДЕПЛОЄНО):** таб «Огляд» editorial-redesign — **народжено Закон темного блоку** (проектуй з концепту з нуля + асиметрична ієрархія герой+решток у КОЖНІЙ секції; маркер провалу = рівномірність; рендер локально власними очима ДО founder). `OverviewBriefing` (темна обкладинка: виручка-герой serif + Δ Pro + інсайт + by-numbers) замінив рівні картки; `FeaturedServices/Products/Clients` (герой+ранг-список); `OverviewDetailSheet` (адаптивний Sheet деталей, payload hero/rows/note/cta); `AnalyticsActivation` (empty-state новачка); декомпозиція 988-рядкового оркестратора → `useAnalyticsMarketing` + `ClientSheetById`. `RevenueLineChart` переписано на **recharts** (🔴 `isAnimationActive={false}` обов'язково — інакше зникає в headless). Усе клікабельне через реальні дані (нуль фейку). Видалено мертвий `ServiceRow.tsx`. TSC:0 · Build clean · a11y (slate 6.09–11.71) · humanizer. **Решта 6 табів → M-ANL-02..07 за `ANALYTICS_TABS_REDESIGN.md` (один таб = одна сесія).** 🔴 До виправлення в табах: VacationTab фейк-кнопка «Оптимізувати», FinancesTab/StockTab dummy-числа під блюром.
> ✅ **M-REVW-01 + M-REVW-02 DONE (commit `206fee3f`, deploy `dpl_D2mLionYHEtGYwAJaw855VRM7TxQ`):** преміум-редизайн сторінки відгуків майстра. **Hero-зріз** (середній рейтинг serif + зірки + «N відгуків» pluralUk) замінив 3 однакові stat-картки; тиха стрічка `Публічні · Приховані` без коробок. **Гістограма-як-фільтр** — розподіл 5★→1★ зі смугами-частками, кожен рядок = клікабельна multi-select кнопка зіркового фільтра (`aria-pressed`, disabled на 0); об'єднує розподіл + фільтр в один елемент замість окремого ряду чіпів. Контролі: пігулки публікації + тогл «Лише з коментарем» + сорт-`select` (Новіші/Старіші/Високий/Низький) + «Скинути»; усі AND в одному `useMemo`. **M-REVW-02:** картки клікабельні → адаптивний `ReviewDetailSheet` (Dialog desktop/vaul mobile, reuse `Sheet`); publish-тогл лишився sibling-кнопкою з `pointer-events-auto` (a11y — не вкладена в клікабельне тіло). **a11y MCP зловив діру:** Frost `muted-foreground` = 2.76:1 на periwinkle (провал малого тексту) → весь дрібний інфо-текст на `text-text-sub` (#475569 = 5.88:1 ✓); зірки/смуги amber 3.90 (графіка 3:1 ✓). Нуль легасі-хексів, тур+Pro-нудж збережені. TSC:0 Build:clean. Очікує візуального QA founder. **Уся секція Reviews закрита.**
> ✅ **M-MKT-05+06 DONE (commit `8bd25847`):** inline per-client деталі через `BroadcastDetailSheet` (vaul-оверлей) замість редіректу на `/marketing/[id]` (роут+`BroadcastDetailPage` видалено; підключено раніше мертвий Sheet). Преміальний редизайн: легасі-peach-хекси→Frost-токени в усіх 3 компонентах; **de-nest аналітики** (nested cards + 7 однакових StatCard = подвійний бан impeccable) → 2 outcome-числа (Записалось/Конверсія) + тиха роздільна стрічка вторинних метрик. Канальна палітра Frost (App=slate/Push=success/Telegram=#2563EB/SMS=warning), іконки 3.89–4.01 на periwinkle (3:1 ✓, сенс дублює форма check/x). tsc 0 (post-build) · build clean · a11y MCP ✓. **M-MKT-05 «inline» = підключити готовий Sheet (3 рядки), реальний обсяг — M-MKT-06.**
> ✅ **Growth (M-GROW-01+02) ЗАДЕПЛОЄНО на прод 2026-06-29** (`dpl_2JosLfqYJRzeG2tb964gvDAEq9Pm`, bookit.com.ua, build 2m READY). DB-симуляція 23/23 + потоки запрошень FLOW1/2/3 (фікс асиметрії `eb7a6f2a`). Очікує візуального QA founder.
> `M-GROW-02` ✅ (commit `31557c87`): повний мердж `master_partners` + `master_alliances` → **нова `master_connections`** (bilateral, kind partner/alliance + role inviter/invitee). `master_referrals` (білінг) НЕ чіпано (рамка founder). Міграція `20260628000008` additive+reversible (старі таблиці лишаються, drop окремо після verify). Backfill: alliance→2 bilateral рядки, dedup partner>alliance, row-count верифіковано (1 alliance→2, dropped=0). **Перенацілено 7 споживачів** (partners.ts/referrals.ts write, getGrowthPageData/[slug]page/wizard/AllianceMap read, 2 тести). **UI:** PartnersPage дві секції → один список мережі з origin-бейджами (Партнер/Реферал), remove лише партнерам. **🔴 Латентний баг пофікшено:** `trustedPartners` на публічній сторінці був мертвий для анонів (стара RLS пускала лише сторони) → `mc_public_read` (is_visible+accepted, anon) оживляє. **Security:** write лише service_role, public-read non-PII opt-in, FK 23503 zone недоторкана, advisor clean. a11y бейдж #3F5C5B 5.02. Розвідка перед брифом виявила: alliance-insert живе у referrals.ts (write-path дотик неминучий), напрям дублює master_referrals, partners-RLS асиметрична. Ритуал: brainstorming→розвідка(2 AskUserQuestion: scope+схема)→spec→senior-backend→create-migration→security-review→a11y. TSC:0 Build:clean, 31 тест pass (4 pre-existing). Drop старих таблиць ВІДКЛАДЕНО. Очікує візуального QA founder.
> `M-GROW-01` ✅ (commit `3cf3deea`): сторінка лояльності з форми налаштувань → панель керування. **Огляд-картка:** тріада тап-сегментів `у прогресі / готові / за крок` (колір на icon-чіпі+числі, не side-stripe) → тап у `/clients` звужений + impact-смуга «₴ віддано · N разів» (30д, forward-only, чесний empty). **Картки програм progress-aware:** двосегментна смуга reached(success)/on_track(primary) + «X на шляху · Y готові», `Y` клікабельне. Info-банер тепер ЛИШЕ в порожньому стані (огляд займає його місце — 3-сек правило). **Бекенд:** `bookings.loyalty_label/amount` (міг `20260628000006`) + `createBooking` пише знижку лояльності (раніше обчислював і викидав, одиниця=гривні); RPC `get_loyalty_overview`(pipeline all-time, total_visits)+`get_loyalty_impact`(30д) — `20260628000007`, auth.uid без IDOR. **Security:** advisor зловив що anon успадковує EXECUTE попри REVOKE public → заблокував явно `REVOKE FROM anon` (строго краще за референс M-REV-05; витоку не було, auth.uid=NULL→порожньо). **A11y mcp:** числа 3.89-3.90 (3:1✓), малі лінки/чіп #0D6B2F 5.16 / #0A5526 6.17 (4.5:1✓). **ClientsPage:** `?loyaltyMin/loyaltyExact` фільтр по total_visits + dismissible чіп. **ВІДОМА діра (документ.):** total_visits тригериться лише для зареєстрованих клієнтів; гостьові візити до реєстрації не входять — взято свідомо (консистентність із /clients). Ритуал: brainstorming→impeccable craft→self-grill→бриф→humanizer→senior-backend→create-migration→a11y→security-review. TSC:0 Build:clean. Math верифіковано на реальному майстрі (3 програми 5–30: 5/2/0). Очікує візуального QA founder.
> `M-REV-06` ✅ (commit `3d7a11ef`): distill інфо-блоку ціноутворення (весь PricingHero). 3 тап-чіпи механіки (Стекінг/Max-30%/Max+50%) + карет-поповер → один тихий рядок-виноска під hairline, завжди видимий: «Правила складаються, але ціна тримається в межах від -30% до +50%». Прибрано `INFO_CHIPS`, стейт `activeChip`, `AnimatePresence` поповер, `caretLeft` (−43 рядки). **Чому:** механіка (ліміти, накладання) — довідка, схована за тапом жаргонними ярликами; ліміти вже демонструють живі `PreviewRow` + картки правил. **A11y-дірка, зловлена grill-me:** текст виноски на `--text-secondary` (#475569 = 5.98:1 AA), НЕ `muted-foreground` (#80889F ефективно = 2.79:1, провалив би 11px). Перевірено a11y MCP. Header/proof-логіка/`AnchoredTooltip` тур недоторкані. Ритуал: `brainstorming`→`impeccable craft`→`grill-me`→бриф→humanizer→код. Очікує візуального QA founder.
> `M-REV-05` ✅ (commit `8aac403e`): статистика по типах ціноутворення. **Частина 1:** огляд-блок «Результати правил» на вкладці Смарт-ціни (після hero) — 4 правила ранжовані (Пік `+₴·N×` warm / знижки `N слотів` cool, сорт за кількістю; 0 → сіре читабельне), тап → наявна `PricingRuleStatsSheet` (reuse). RPC `get_pricing_rules_overview()` (auth.uid, без IDOR), блок ховається без даних. **Частина 2 (фікс наявного):** `get_dynamic_pricing_uplift` переписано — матч по ТИПУ (прибрано фрагментацію по повному лейблу '🔥 Пік +20%' + мертвий мапінг `rule==='peak'`), прибрано markup-only фільтр, +`saved_slots`; віджет `DynamicPricingUplift` тепер показує надбавку ₴ І врятовані слоти + чисті назви правил; проведено через useAnalyticsExtras→AnalyticsPage→BentoSecondary. Міграції `20260628000004`+`20260628000005`. Свідомо: огляд = all-time (confirmed+completed), віджет = за період (incl pending). Founder QA: «вогонь».
> `M-REV-04` ✅ (commit `c0c9020a`): смарт-ціни преміальний редизайн + стата по правилах. **База:** hero «Ціни, що працюють без тебе» + доказ-рядок; 4 правила → 2 семантичні секції (warm «Заробити» / cool «Заповнити»); `border-l-4` side-stripe (бан impeccable) → іконка-чип несе колір; усі легасі-хекси → Frost-токени; `PricingUpgradeGate` 3 view токенізовано (повний редизайн, founder). Пре-код ритуал brainstorming→impeccable craft→grill-me відпрацював (грил зловив: доказ-цифра рахує лише надбавку). **Follow-up founder (4 блоки):** (A) тап-тултіпи на інфо-чіпах (overflow-safe карет-поповер замість мертвого `title=`); (B) врятовані слоти у hero — read-side `getDynamicPricingSavedSlots`, **закрило діру грилу** (майстер на самих знижках бачив порожній рядок при earned=0); (C) прев'ю по типу правила на рівень секцій (не на кожній картці); (D) **модалка статистики по правилу** — RPC `get_pricing_rule_stats` (фільтр `auth.uid()`, **без IDOR**), `BarChart3` sibling-button (a11y: 3 окремі кнопки), `PricingRuleStatsSheet` (кількість/₴/сер.%/дата/останні записи). Міграція `20260628000003`. **Часткове тягнення M-REV-05** (per-rule стата). a11y: числа foreground, колір на іконках/тінтах. Очікує візуального QA founder.
> `M-REV-03` ✅ (commit `255bbcf3`): детальна статистика флеш-акцій. Тап по активній акції → `FlashDealDetailSheet` (тип Авто/Вручну, claimed-конверсія, список сповіщених по каналах). Нова `flash_deal_recipients` (дзеркало `broadcast_recipients`) + RLS, пишеться при відправці спільним `notifyAndRecordFlashDeal` (прибрав дубль notify-блоку ручний/авто). Тип через **reuse `booking_id`** (авто пише id звільненого запису, ручний null — без нової колонки). `getFlashDealStats` + `useFlashDealStats`. Per-channel прапорці виведено з наявності push-підписки/telegram_chat_id (флеш шле bulk-push, не по-клієнтно). a11y бейдж amber-700 (5.02). **Чесний нюанс founder:** ~6 старих активних акцій → порожній стан «доставку не відстежували» (дані ніколи не збирались). Міграція `20260628000002`.
> `M-REV-02` ✅ (commit `255bbcf3`): авто-flash працездатність A+B+C + 2 баги. **A** таргетинг: обидва виклики RPC → м'який 1-арг, 3-арг строгий оверлоид **дропнуто** (`20260628000001`) — кінець триразового рецидиву сигнатури; ініціатор скасування виключений (excludeClientId). **B** інверсія дефолту founder: клієнт скасовує → авто-flash ОБОВ'ЯЗКОВО (`after()`), майстер скасовує → глобальна confirm-шторка «Слот звільнився». 3 шляхи скасування (BookingCard/Dropdown/DetailsModal) → один zustand-стор → одна шторка в `DashboardLayout` (per-card шторка демонтувалась разом зі скасованою карткою — корінь «промту не було»). **C** надійність: notify+авто-flash у `after()`. **BUG тогл Auto Flash не зберігався:** MasterContext + layout select без `auto_flash_*` колонок → `useEffect` скидав у false; додано колонки в обидва select + у тип `MasterProfile`. diagnose з БД (Vercel-логи порожні через проковтнуту помилку).
> `M-REV-01` ✅ (commit `6931549a` редизайн + `013095ef` хаб-шелл): флеш-акції преміальний редизайн (Фаза 3 старт). FlashDealPage: hero вигоди serif «Вільне вікно? Заповни знижкою.» + 1 амбер-іскра, грошовий нудж-виручка «+X₴ за порожній слот», **живі таймери** на активних (`timeUntil(expires_at)`, 1 спільний інтервал/хв, expired→«Завершується»), усі легасі-хекси→токени, CTA помаранч→slate `bg-primary`, vanity-метрики→живе «N акцій працює». **Розширено founder:** хаб-шелл `RevenueHubClient` — шапка понижена (де-твін з hero), tab-overflow виправлено (текст-онлі моб + nowrap + px-5→px-3, іконки hidden lg). Ритуал: brainstorming→impeccable craft→grill-me→design-taste-frontend. a11y: таймер text-foreground (warning тінт <4.5). **+ окремий BUGFIX `7b6375f8`:** `get_eligible_flash_deal_clients` виклик мав неіснуючу сигнатуру (p_master_id,p_slot_timestamp) → проковтнута помилка → 0 нотифікацій завжди (РЕЦИДИВ — той самий баг фіксили в `bb9dac0e`). Фікс: +p_service_id у обидва виклики + error-лог + 3-арг RPC повертає client_name. 0→3 клієнти на БД.
> `M-SHOP-03b` ✅ (commit `9f97b5a5`): відгуки про товари на сторінці товару. **Контртеза до спеки:** нова таблиця НЕ потрібна — `reviews` уже має `order_id`+`product_id`, `submitReview` уже збирає order-відгуки (is_published=false, майстер модерує), `MyBookingsPage`/`ShopOrderCard` уже має UI «Поділитись враженнями». Бракувало лише READ-SIDE. Реалізовано: RPC `get_product_reviews(product_id)` derive через `reviews.order_id → order_items.product_id` (як `get_service_reviews` для послуг; мультитоварне замовлення → відгук під кожним товаром), SECURITY DEFINER hardened, індекси вже були, smoke-test ✓. Хук `useProductReviews`. `ProductDetailView`: заглушку → живий блок (avg+Stars+список+loading+empty). Збір/таблицю не чіпано; `product_id` лишається невикористаним (per-order derive, рішення founder). Міграція `20260627000010` (MCP+локально).
> `M-SHOP-03` (A+B) ✅ (commit `19bd7894`): **сторінка товару = окремий роут** `/[slug]/shop/[productId]` (SSR + OG-метадані). Кошик піднято у `ShopCartProvider` (`shop/layout.tsx`) + `localStorage['bookit_cart_${slug}']` — переживає навігацію каталог↔товар і reload (hydration-safe). `ShopCartBar` (sticky+checkout+success) винесено зі ShopPage, читає контекст; рендериться на обох сторінках (активна одна за раз). `ProductDetailView` — спільний презентаційний (галерея свайп/стрілки/крапки/thumbnails + назва/ціна/залишок/опис + порожня секція відгуків). `ProductPage` = view + qty + в кошик. `ShopPage` рефакторено: тайл→`Link`, `ProductDetailSheet` видалено, checkout у ShopCartBar. **Phase B:** ProductCard майстра отримав Eye-прев'ю → `ProductDetailView` mode=master read-only у Sheet (нудж порожнього опису). **Розбито (founder):** відгуки (Фаза C) винесено в `M-SHOP-03b`; на сторінці поки статичний порожній стан «Відгуків поки немає». Бекенд createOrder/checkout не чіпано (лише підняття стану кошика). total 77→78 (+M-SHOP-03b).
> `M-SHOP-02` ✅ (commit `4d428d28`): картки товарів маркетплейс + **2 режими** (сітка/список, перемикач у сайдбарі + `localStorage['products_view']`). `ProductCard` переписано 1:1 з M-SVC-02: сітка = вертик. плитка фото-зверху aspect-[16/10] (Frost icon-fallback) + **glass-піл залишку оверлеєм top-right** (колір success/warning/destructive) + footer-дії; список = горизонт. рядок, назва на всю ширину + правий стовпчик ціна-над-діями. Тап по тілу → редактор (рішення founder), аналітика винесена в окрему кнопку `BarChart3` поряд із Поповнити/Редагувати (повнокарткову z-0 підкладку прибрано). Спільні `actions`+`toggle`, DnD працює в обох режимах (Droppable на grid). Бекенд/хуки/RPC/розхідники не чіпані.
> `M-SHOP-01` ✅ (commit `641141d3`): аналітика по товару — `getProductStats` рахує ОБИДВА канали продажів (order_items + booking_products); блок «Аналітика продажів» у ProductEditor + overlay Sheet з картки (a11y sibling-button підкладка); спільний `ProductStatsPanel`. **+ Аудит товарів/розхідників (UX→БД): P1 5/5 + P2 4/5 закрито в тому ж коміті.** P1: витік собівартості (колонкові GRANT anon), порожня історія складу (політика `pt_master_select`), restock `booking_products` при скасуванні, idempotency `completeBooking`. P2: atomic RPC `deduct_consumable_stock`, тип `'deduction'`, drop permissive INSERT orders/order_items, emoji→Info. 13 тестів. Міграції `20260627000001-05` (MCP+локально). Відкладено: P2 #9 vaul ShopPage + P3 advisors.
> `M-SVC-03` ✅ (commit `e2973465`): детальна «картка товару» з описом + **відгуками по послузі**. БД: RPC `get_service_reviews(service_id)` SECURITY DEFINER — відгуки виводяться через `reviews.booking_id → booking_services.service_id` (bookings.service_id не пишеться). Мультипослуговий запис: відгук візиту показується під кожною послугою (свідоме рішення founder). Клієнт: акцентна кнопка «Детальніше» (тап=вибір лишається) → `ServiceDetailSheet` (темний hero-блок serif-назва поверх — єдиний контраст фото+icon-fallback, ціна фокусне число, опис, рейтинг+відгуки, CTA «Обрати»). Майстер: Eye-прев'ю в обох режимах → той самий Sheet read-only + нудж «Додайте опис». impeccable bolder+polish, контраст AA, security-review clean. Без зміни схеми reviews.
> `M-SVC-02` ✅ (commit `980b5402`): картки маркетплейс + **2 режими перегляду** (сітка/список, перемикач у сайдбарі + localStorage). Сітка = вертикальна плитка фото-зверху aspect-[16/10] (Frost icon-fallback) + footer-дії; список = горизонт. рядок, назва на всю ширину (line-clamp-2, без скорочень), правий стовпчик ціна-над-діями. `view` проп + спільні editDelete/toggle блоки. 3 ітерації founder: гібрид→горизонт→вертикаль + ad-hoc другий режим. Бейдж «Хіт» на популярних. Бекенд/DnD/поля не чіпані.
> `M-DASH-12` додано поза беклогом (ad-hoc founder): вирівнювання висоти десктоп-блоків + кольори → total 76→77. **Вирівнювання висоти лишилось. РЕВІЗІЯ `676c191b` (2026-06-25): бари WeeklyChart відкочено з мультиколору до монохрому + поглиблено рампу обох віджетів (WeeklyChart + PeakHours) до сіро-чорної ~34→100%. Відкрите питання founder закрито.**
> 🧪 **Мультиагент згорнуто (рішення founder 2026-06-25):** пілотна хвиля 1 (M-DASH-09 + M-SET-01) відпрацювала, але виграш ~break-even на дрібних задачах. Авто-нудж-хук + worktree baseRef прибрано. Машинерія описана в `PARALLEL_WORKFLOW.md` §7 — лишається як довідка, не активна.
> `G-LAND-02` закрито поза чергою як тестовий прогін воркфлоу (Тір 0).
> `M-DASH-03` scroll-UX був покритий G-PWA-02 → закрито motion-полишем (stagger груп слотів).
> `M-CLI-04` scroll-UX покрито G-PWA-02 → закрито перевіркою без коду: retention-чіпи + кастомні сегменти у `ClientsPage.tsx` вже обгорнуті в `ScrollStrip` (рядки 286, 319).
> `M-BOOK-01` зроблено ПОЗА ЧЕРГОЮ (founder: «по гарячим слідам» одразу за M-CLI-05) — той самий пастельний glow на `BookingCard`. Формула винесена у спільний `lib/utils/statusGlow.ts`.
> `M-CLI-06` ✅ (commit `1f05146a`): профіль клієнта виявився СПІЛЬНИМ компонентом (6 точок) → один редизайн покрив clients/dashboard×3/StatsModals/analytics; BookingDetailsModal-дубль підтягнуто екстракцією (ClientIdentityHeader + ClientStatChips). Реальний LTV (total_spent+ранг+каденс) + реальні мітки (міграція vibe_tags text[]).
> `M-BOOK-05` ✅ (commit `0ebd850b`): деталь запису лишилась adaptive Sheet (не route). Receipt-картка (hero serif-дата + час tabular + source-чіп → пунктир → рядки → «Разом» serif 3xl). Новий status-outcome блок для термінальних (іконка + Завершено/Скасовано/Клієнт не прийшов + `status_changed_at` + `cancellation_reason` — раніше мертві поля). Термінальні дії «Записати знову» (UrlActionBus) + «Профіль клієнта». a11y: статус-лейбл пастель→text-foreground (контраст <4.5). Бекенд/хук/ReschedulePanel не чіпано. Очікує візуального QA founder.
> `M-BOOK-03`+`M-BOOK-04` ✅ (commit `757bcb89`): 4 верхні віджети bookings → клікабельні кнопки + adaptive Sheet з розбивкою; елементи списків ведуть на main-елемент (клієнт→clients?clientPhone, запис→?bookingId). Нуль нових запитів (хук +2 поля). M-BOOK-04: кнопка «Новий запис» a11y (aria-label+текст) + компактний pill (заголовок/кнопка ~70/30, founder).
> `M-BOOK-02` ✅ (commit `811482da`, 3 ітерації, ескалація Sonnet→Opus): bolder таймлайн = спец-блок `TimelineBlock` (статус-рейка + твердий часовий каркас + герой now-line) + **Smart Design System** — наповнення адаптується під висоту блока (sm 1 рядок / md-lg top-anchored / xl 1год+ повна rich-картка з тривалістю+ціна-футер). Бокові години узгоджено зі шрифтом часу на картках (sans tabular). Тіла лишились пастельними (M-BOOK-01 не відкочено).
> `M-DASH-04` ціль = віджет «Записи» (TodaySchedule на дашборді), НЕ сторінка /bookings. Скоуп: лише цей віджет, усі таби.
> `M-DASH-10` додано поза беклогом (ad-hoc від founder): «Записи» header uppercase + багатий порожній стан → total 74→75.
> `M-DASH-11` додано поза беклогом (ad-hoc від founder): «Пікові години» heat-палітра як у WeeklyChart → total 75→76.

> Порядок нижче = рекомендований порядок виконання (фази). Групування за зонами — у `BACKLOG.md`.
> Зони: **M-** Майстер · **C-** Клієнт · **G-** Глобальне. `P` = пріоритет.

**Легенда моделей:**
`Haiku` — pure copy/text, humanizer only · `Sonnet` — стандарт (дефолт) · `Sonnet→Opus` — старт Sonnet, ескалувати якщо root cause глибший · `Opus` — складні баги / архітектура / full 🔄 редизайни

---

## ФАЗА 0 — P0 БАГИ (блокери)

| ID | Задача | P | Ст | Спеціаліст-скіли | Модель | Commit |
|----|--------|---|----|------------------|--------|--------|
| `M-SVC-01` | Послуги: статистика по послузі (read-side, не backend) | P0 | ✅ | `senior-backend` | **Opus** | `028e6820` |
| `M-DASH-06` | Пікові години: тултіп спрацьовує з 2-го тапу | P0 | ✅ | `diagnose` → `senior-frontend` | **Sonnet→Opus** | `f0a91bc5` |
| `M-SHOP-04` | Магазин: модалка поповнення → vaul + собівартість | P0 | ✅ | `senior-frontend` (vaul) | **Sonnet** | `98e89c52` (+hotfix `62c7da75`) |
| `G-LOGIN-02` | Логін мобільний: iOS-клавіатура — visualViewport fixed shell (re-open, фінал) | P0 | ✅ | `senior-frontend` | **Opus** | `ff209529` |
| `G-LAND-02` | Лендинг: мобільна шапка, кнопка + відступи | P0 | ✅ | `design-taste-frontend` | **Haiku** | `8a8ad674` |

## ФАЗА 1 — Глобальні основи

| ID | Задача | P | Ст | Спеціаліст-скіли | Модель | Commit |
|----|--------|---|----|------------------|--------|--------|
| `G-PWA-02` | Уніфікація горизонтальних скролів (парасолька) → `ScrollStrip` (fade + 1-крок стрілки + крапки на елемент) | P1 | ✅ | `scroll-experience` + `design-taste-frontend` | **Sonnet→Opus** | `ae9466d8` |
| `G-PWA-01` | Скляна Safe Area (blur/backdrop при скролі) | P1 | ✅ | `progressive-web-app` + `scroll-experience` | **Sonnet** | `56ed454c` |

## ФАЗА 2 — Зона Майстра: щоденні екрани

| ID | Задача | P | Ст | Спеціаліст-скіли | Модель | Commit |
|----|--------|---|----|------------------|--------|--------|
| `M-DASH-01` | Дашборд: динамічні блоки рекомендацій (top) | P1 | ✅ | `design-taste-frontend` + `impeccable (layout)` | **Sonnet** | `d857a5e6` |
| `M-DASH-02` | Дашборд: Quick Actions tap-анімація | P2 | ✅ | `emilkowalski-motion` | **Sonnet** | `dc5df938` |
| `M-DASH-03` | Дашборд: "Вільно сьогодні" scroll UX (scroll=G-PWA-02; +motion-полиш груп) | P1 | ✅ | `emilkowalski-motion` | **Opus** | `4d6c2dcf` (+tweak `762461a3`) |
| `M-DASH-04` | Дашборд: "Записи" — прибрати капс у текстах (віджет TodaySchedule, всі таби) | P2 | ✅ | `humanizer` | **Haiku** | `b18512b4` |
| `M-DASH-05` | Дашборд: "Доходи і записи" — колоризація + fix "грн" | P1 | ✅ | `impeccable (colorize)` | **Sonnet** | `15e7bf3b` |
| `M-DASH-10` | Дашборд: "Записи" — uppercase header + багатий порожній стан з CTA | P1 | ✅ | `impeccable (colorize)` + `humanizer` | **Opus** | `0e40b5b9` |
| `M-DASH-11` | Дашборд: "Пікові години" — heat-палітра як у WeeklyChart (0=нейтрал) | P1 | ↩️ | `impeccable (colorize)` | **Opus** | `981ee824` → СКАСОВАНО `90260003` (founder: повернути сіро-чорні) |
| `M-DASH-12` | Дашборд десктоп: вирівняти блоки по висоті (✅) + кольори (відкочено до монохрому) (ad-hoc) | P1 | ✅ | `design-taste-frontend` + `impeccable (colorize)` | **Opus** | `649d9341` (висота) · ревізія `676c191b` (бари+PeakHours монохром, рампа 34→100%) |
| `M-DASH-07` | Дашборд: "Скасування" — overlay хто/коли | P1 | ✅ | `senior-frontend` | **Sonnet** | `b970066a` |
| `M-DASH-08` | Дашборд: "Середній чек" — overlay розбивка по послугах | P1 | ✅ | `senior-frontend` | **Sonnet** | `37f8ca65` |
| `M-DASH-09` | Дашборд десктоп: квадратний календар + реферали поряд | P1 | ✅ | `design-taste-frontend` + `impeccable (layout)` | **Sonnet** | `a0614a7c` (wave-1) |
| `M-CLI-01` | Клієнти: grid-картки єдиний лейаут | P1 | ✅ | `impeccable (layout)` + `design-taste-frontend` | **Sonnet** | `94515808` |
| `M-CLI-02` | Клієнти: віджет "Важливі/Амбасадори" свайп | P1 | ✅ | `emilkowalski-motion` | **Sonnet** | `72a92ac1` |
| `M-CLI-03` | Клієнти: інфо-меседжі з dismiss 12год | P2 | ✅ | `senior-frontend` + `mark-as-read-on-close` | **Sonnet** | `10038f6b` |
| `M-CLI-04` | Клієнти: мобільні статуси/теги scroll UX | P1 | ✅ | покрито `G-PWA-02` (ScrollStrip) | — | ↗ G-PWA-02 |
| `M-CLI-05` | Клієнти: кольорова корекція карток (пастель) | P1 | ✅ | `impeccable (distill + colorize)` | **Sonnet** | `fa34fb9d` |
| `M-CLI-06` | Клієнти: сторінка клієнта (деталі) — глибокий редизайн 🔄 + спільні під-компоненти + реальний LTV/мітки | P1 | ✅ | `design-taste-frontend` + `impeccable` + `humanizer` | **Opus** | `1f05146a` |

| `M-BOOK-01` | Записи: кольорова корекція карток (пастель) | P1 | ✅ | `impeccable (distill + colorize)` | **Sonnet** | `7777a7dc` |
| `M-BOOK-02` | Записи: таймлайн на день (bolder) + Smart Design System (адаптив за висотою) | P1 | ✅ | `impeccable (bolder)` + `design-taste-frontend` | **Opus** | `811482da` |
| `M-BOOK-03` | Записи: верхні віджети клікабельні + overlay (елементи → main-елемент) | P1 | ✅ | `senior-frontend` | **Sonnet** | `757bcb89` |
| `M-BOOK-04` | Записи: "Новий запис" — a11y + компактна кнопка (70/30) | P2 | ✅ | `senior-frontend` | **Sonnet** | `757bcb89` |
| `M-BOOK-05` | Записи: деталь запису — редизайн 🔄 (receipt+hero, status-outcome, термінальні дії) | P1 | ✅ | `design-taste-frontend` + `impeccable` | **Sonnet→Opus** | `0ebd850b` |
| `M-SVC-02` | Послуги: картки у стилі маркетплейсу + 2 режими (сітка/список) | P1 | ✅ | `design-taste-frontend` + `impeccable` | **Sonnet** | `980b5402` |
| `M-SVC-03` | Послуги: режим "картка товару" (опис+відгуки) + для клієнтів 🔄 | P1 | ✅ | `spec-driven-workflow` → `create-migration` → `security-review` → `impeccable` | **Opus** | `e2973465` |
| `M-SHOP-01` | Магазин: аналітика по кожному товару (+ аудит P1×5/P2×4) | P1 | ✅ | `senior-backend` + `security-review` + `create-migration` | **Opus** | `641141d3` |
| `M-SHOP-02` | Магазин: картки товарів у стилі маркетплейсу + 2 режими (сітка/список) | P1 | ✅ | `design-taste-frontend` + `impeccable` | **Sonnet** | `4d428d28` |
| `M-SHOP-03` | Магазин: сторінка товару (роут) + клієнт-сторінка + майстер-прев'ю (A+B) 🔄 | P1 | ✅ | `design-taste-frontend` + `impeccable` | **Sonnet→Opus** | `19bd7894` |
| `M-SHOP-03b` | Магазин: відгуки про товари (показ на сторінці товару — RPC derive через order_items; збір уже існував) | P1 | ✅ | `create-migration` + `security-review` | **Sonnet** | `9f97b5a5` |

## ФАЗА 3 — Зона Майстра: інструменти росту

| ID | Задача | P | Ст | Спеціаліст-скіли | Модель | Commit |
|----|--------|---|----|------------------|--------|--------|
| `M-REV-01` | Revenue: флеш-акції преміальний редизайн (+хаб-шелл) | P1 | ✅ | `design-taste-frontend` + `impeccable-design-polish` | **Sonnet** | `6931549a`+`013095ef` |
| `M-REV-02` | Revenue: авто-flash працездатність (A таргетинг + B клієнт/майстер тригер + C надійність) + bug тогл | P1 | ✅ | `diagnose` + `senior-backend` | **Opus** | `255bbcf3` |
| `M-REV-03` | Revenue: детальна статистика флеш-акцій (тип/claimed/канали) | P1 | ✅ | `senior-backend` + `create-migration` | **Sonnet→Opus** | `255bbcf3` |
| `M-REV-04` | Revenue: смарт-ціни преміальний редизайн (+тултіпи чіпів, врятовані слоти, прев'ю по типу, модалка стати по правилу) | P1 | ✅ | `brainstorming` → `impeccable craft` → `grill-me` → `design-taste-frontend` + `create-migration` | **Opus** | `c0c9020a` |
| `M-REV-05` | Revenue: статистика по типах ціноутворення (огляд-блок + фікс аналітик-віджета) | P1 | ✅ | `senior-backend` + `create-migration` + `design-taste-frontend` | **Opus** | `8aac403e` |
| `M-REV-06` | Revenue: distill інфо-блоку "ціноутворення" (весь PricingHero → тиха виноска) | P2 | ✅ | `impeccable (distill)` + `humanizer` + `mcp__a11y` | **Sonnet→Opus** | `3d7a11ef` |
| `M-GROW-01` | Ріст: лояльність преміальний редизайн + стата | P1 | ✅ | `senior-backend` + `design-taste-frontend` + `create-migration` | **Sonnet→Opus** | `3cf3deea` |
| `M-GROW-02` | Ріст: об'єднати реферали + партнери (HARD) | P1 | ✅ | `senior-backend` + `create-migration` + `security-review` (improve-codebase-architecture недоступний) | **Opus** | `31557c87` |
| `M-MKT-01` | Маркетинг: сторіс у рівний грід ↩️ поглинуто M-MKT-04 (редизайн story-едітора — старе превʼю-розкладка більше не існує) | P1 | ↩️ | — | — | `e8837dba` |
| `M-MKT-02` | Маркетинг: зменшити превью сторіс на 30% ↩️ поглинуто M-MKT-04 (превʼю тепер єдиний live-canvas) | P2 | ↩️ | — | — | `e8837dba` |
| `M-MKT-03` | Маркетинг: додати кольорів до палітри | P2 | ✅ | `impeccable (colorize)` | **Sonnet** | `05e03119` |
| `M-MKT-04` | Маркетинг: проф-едітор сторіс покроковий 🔄 (+ітерації founder: образи-пресети, авто-a11y, no-gradients) | P1 | ✅ | `spec-driven-workflow` → `senior-frontend` + `emilkowalski-motion` + `brainstorming` + `humanizer` + `a11y MCP` | **Opus** | `e8837dba` |
| `M-MKT-05` | Маркетинг: розсилки — статистика inline + деталі по клієнтах inline (Sheet, без редіректу) | P1 | ✅ | `senior-frontend` + `impeccable` | **Sonnet→Opus** | `8bd25847` |
| `M-MKT-06` | Маркетинг: преміальні картки розсилок (Frost-токени + de-nest аналітики) | P1 | ✅ | `design-taste-frontend` + `impeccable` | **Sonnet→Opus** | `8bd25847` |
| `M-REVW-01` | Відгуки: редизайн + фільтрація/сортування | P1 | ✅ | `design-taste-frontend` + `impeccable` + `humanizer` + `mcp__a11y` | **Opus** | `206fee3f` |
| `M-REVW-02` | Відгуки: клікабельні картки → деталі (Sheet) | P2 | ✅ | разом з M-REVW-01 (`ReviewDetailSheet`) | **Opus** | `206fee3f` |
| `M-ANL-01` | Аналітика: таб «Огляд» editorial-redesign (Закон темного блоку) | P1 | ✅ | `spec-driven-workflow` → `design-taste-frontend` + `impeccable-design-polish` | **Opus** | `2c48474f` |
| `M-ANL-02` | Аналітика: таб «Джерела» (`source`) редизайн | P1 | ✅ | `design-taste-frontend` + `impeccable` + `humanizer` + `mcp__a11y` | **Opus** | `100b94b2` |
| `M-ANL-03` | Аналітика: таб «Відгуки» (`reviews`) редизайн | P1 | ✅ | `design-taste-frontend` + `impeccable` + `humanizer` + `mcp__a11y` | **Opus** | `ccebf398` |
| `M-ANL-04` | Аналітика: таб «Склад» (`stock`) редизайн + фікс dummy-чисел | P1 | ✅ | `design-taste-frontend` + `impeccable` + `humanizer` + `mcp__a11y` | **Opus** | `924105d6` |
| `M-ANL-05` | Аналітика: таб «Фінанси» (`finances`) редизайн + фікс dummy-чисел | P1 | ✅ | `design-taste-frontend` + `impeccable` + `humanizer` + `mcp__a11y` | **Opus** | `bbd45815` |
| `M-ANL-06` | Аналітика: таб «Поведінка» (`behavior`) редизайн + фікс фейк-кнопки Vacation | P1 | ✅ | `design-taste-frontend` + `impeccable` + `humanizer` + `mcp__a11y` | **Opus** | `f21d84fd` |
| `M-ANL-07` | Аналітика: таб «Зростання» (`growth`) редизайн | P1 | ✅ | `design-taste-frontend` + `impeccable` + `humanizer` + `mcp__a11y` | **Opus** | `987b3d67` |

## ФАЗА 4 — Зона Майстра: налаштування + допоміжне

| ID | Задача | P | Ст | Спеціаліст-скіли | Модель | Commit |
|----|--------|---|----|------------------|--------|--------|
| `M-SET-01` | Налаштування: "Графік роботи" нижче інфо профілю | P2 | ✅ | `design-taste-frontend` | **Haiku** | `0f19b843` (wave-1, mobile order-*) |
| `M-SET-02` | Налаштування: дизайн блоку інфо профілю (ProfileHero «Опублікована обкладинка») | P1 | ✅ | `impeccable` (craft) | **Opus** | `ded4332d` |
| `M-SET-03` | Налаштування: bookit assistant активний + лінки (SmartAdvisor «Наступний крок» — поради→скрол+підсвітка, фікс каталог-фейку) | P1 | ✅ | `impeccable` (craft) + `humanizer` | **Opus** | `721d9182` |
| `M-SET-04` | Налаштування: відпустки + вихідні редизайн 🔄 (об'єднано з тижневими днями; тумблер дня переїхав зі ScheduleWidget) | P1 | ✅ | `impeccable` (craft) | **Opus** | `207f1955` |
| `M-SET-05` | Налаштування: логіка заповненості по днях + смарт-статус (BUGFIX: 5 станів замість бінарного «перерва») | P1 | ✅ | `diagnose` (root cause) | **Opus** | `c8b4d9ff` |
| `M-BILL-01` | Тариф: повний редизайн + бренд Monobank (state-aware герой, офіційний SVG-лого, плани→тихі рядки) | P1 | ✅ | `brainstorming` + `impeccable (craft)` + `mcp__a11y` + `humanizer` | **Opus** | `441e1e1f` |
| `M-BILL-02` | Тариф: чесний перелік фіч (аудит tier-гейтів) + Pro домінантний + Studio-бета пункт | P2 | ✅ | `multi-perspective-analysis` + `humanizer` + `design-taste-frontend` | **Opus** | `b668dd55` |
| `M-PORT-01` | Портфоліо: стандартизувати розмір карток (вирівняти висоти + полиш токенів/a11y) | P1 | ✅ | `impeccable-design-polish` (polish+colorize) | **Opus** | `e724e5ef` |
| `M-PORT-02` | Портфоліо: відгуки з пагінацією (редактор майстра: пошук + пагінація вибору відгуків) | P2 | ✅ | `senior-frontend` + `humanizer` | **Opus** | `73a697a6` |
| `M-ORD-01` | Замовлення: сортування (сума/час) — select у вкладці Замовлення ProductsPage | P2 | ✅ | `senior-frontend` | **Opus** | `4752ea10` |
| `M-DOC-01` | Документи: impeccable quieter + distill (тихі картки 2×2, єдиний Frost-нейтрал) | P2 | ✅ | `impeccable-design-polish` (quieter+distill) | **Opus** | `7fc7fffb` |
| `M-HELP-01` | Підтримка/Академія: дедуп + навігація (FAQ→таб «Питання» в Академії; Support→комунікація) | P1 | ✅ | `design-taste-frontend` + `humanizer` | **Opus** | `b475f465` |
| `M-HELP-02` | Підтримка: зручна комунікація + єдиний інбокс у навігації | P2 | ✅ | `impeccable` + `humanizer` | **Sonnet** | `aa7944f0` |

## ФАЗА 5 — Клієнтська Зона

| ID | Задача | P | Ст | Спеціаліст-скіли | Модель | Commit |
|----|--------|---|----|------------------|--------|--------|
| `C-NAV-01` | MyBottomNav FAB redesign | P1 | ✅ | `emilkowalski-motion` + `design-taste-frontend` | **Sonnet** | `ea1551b6` |
| `C-EXPL-01` | /explore + навбар: повний редизайн 🔄 | P1 | ✅ | `design-taste-frontend` + `emilkowalski-motion` | **Opus** | `373e9701` |
| `C-EXPL-02` | MasterCard + MasterListCard redesign | P1 | ✅ | `design-taste-frontend` + `impeccable (layout)` | **Sonnet** | `373e9701` (злито в C-EXPL-01) |
| `С-CLI-01` | Клієнти: сторінка клієнта — повний редизайн 🔄 | P1 | ⬜ | `design-taste-frontend` + `impeccable` | **Opus** | — |
| `C-BOOK-01` | /my/bookings premium + Review/Cancel Sheet | P1 | ⬜ | `design-taste-frontend` + `emilkowalski-motion` | **Sonnet** | — |
| `C-PROF-01` | /my/profile Identity Card redesign | P1 | ⬜ | `design-taste-frontend` + `impeccable` | **Sonnet** | — |
| `C-MSG-02` | /my/messages: "Мої майстри" рейка + hairline під тайтлом | P1 | ✅ | `impeccable (craft)` + `scroll-experience` + `humanizer` | **Opus** | `a7ed44ce` |
| `C-MSG-01` | /my/messages: UI redesign + keyboard UX | P1 | ⬜ | `senior-frontend` + `design-taste-frontend` + `emilkowalski-motion` | **Sonnet→Opus** | — |
| `C-MAST-01` | /my/masters + loyalty + notifications | P1 | ⬜ | `design-taste-frontend` + `impeccable` | **Sonnet** | — |
| `C-PHONE-01` | /my/setup/phone onboarding redesign | P1 | ⬜ | `auth-implementation-patterns` + `design-taste-frontend` | **Sonnet** | — |
| `C-DESK-01` | Клієнт-зона: десктоп-лейаут 8 сторінок | P1 | ⬜ | `impeccable (layout)` + `design-taste-frontend` | **Opus** | — |

## ФАЗА 6 — Лендинг

| ID | Задача | P | Ст | Спеціаліст-скіли | Модель | Commit |
|----|--------|---|----|------------------|--------|--------|
| `G-LAND-01` | Лендинг: блок "Для кого" (спеціалізації) | P1 | ⬜ | `landing-page-guide-v2` + `humanizer` | **Sonnet** | — |
| `G-LAND-03` | Лендинг: impeccable full pipeline + guide v2 | P1 | ⬜ | `impeccable` (full) + `landing-page-guide-v2` | **Sonnet** | — |
| `G-LOGIN-01` | Логін: копірайт-редизайн | P1 | ⬜ | `humanizer` | **Haiku** | — |

---

## Воркфлоу: ONE TASK = ONE SESSION
1. Прочитати `HANDOFF.md` — знайти ▶ NEXT задачу
2. Startup: `mempalace_status` → `SYSTEM_MAP` → `TRACKER.md`
3. QA Gate → Skill → код → tsc → build → deploy
4. Після deploy: оновити `TRACKER.md` + `HANDOFF.md` + `TRANSITION_PROMPT.md`
