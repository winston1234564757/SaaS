# M-BILL-02 — Тариф: Pro домінантним + чесний опис функціоналу

**Тип:** REDESIGN + COPY (виріс із чистого COPY: додались accordion-взаємодія + landing layout)
**Пріоритет:** P2
**Статус:** ~~DRAFT~~ → ~~APPROVED~~ → **DONE** (2026-07-01, commit `b668dd55`)
**Спеціаліст-скіли:** `humanizer` + `design-taste-frontend`

---

## Поточний стан
- `bookit/src/components/master/billing/BillingPage.tsx` — редизайн M-BILL-01. Pro = герой col-8, Starter/Studio = тихі hairline-рядки «Порівняти тарифи». `PLANS[].features` для Starter/Studio ВИЗНАЧЕНІ, але не рендеряться (мертвий код). Hero-Pro рендерить `PRO_FEATURES` (9, неповний + неточний).
- `bookit/src/components/landing/LandingPricing.tsx` — 3 повні картки (Starter 8 / Pro 13 / Studio 5), Pro accent + `md:-mt-4`, картки рівної висоти (`h-full` + grid stretch). Pro НЕ 2x довший.
- Два переліки фіч (billing 9 vs landing 13) розходяться між собою.

## Аудит tier-гейтів (джерело правди — живий код, не вгадування)
Перевірено enforcement по ~13 фічах. Виправлено 5+ хибних Pro-заявок.

**Доступно на Starter (перевірено — НЕ Pro-ексклюзив):**
- Мультипослугові записи — `createBooking.ts`, без гейта
- Програма лояльності — `createBooking.ts:290`, застосовується всім; капа програм не знайдено
- Розумні ціни (динамічне ціноутворення) — `createBooking.ts:270`, Starter безкоштовно **до 1000 ₴ заробітку** (тріал)
- Пряма переписка (T-chat) — гейта не знайдено
- QR-код — `SharePageCard.tsx`, без гейта

**Справжні Pro-розблокування (реальні гейти):**
- Необмежені записи — `createBooking.ts:179` (Starter кап 40)
- Сторінка без мітки Bookit — `PublicMasterPage.tsx:1077` (вотермарка лише starter)
- Розширена аналітика (історія/прогноз/дельти) — `AnalyticsPage.tsx:111` `isLockedDateRange`
- Магазин на публічній сторінці — `PublicMasterPage.tsx:792` (pro/studio)
- Кастомна тема — `TechnicalIsland.tsx:49`
- Стилі сторіс без розмиття — `useStoryEditor.ts:157` (blur для starter)
- Флеш (5→∞), розсилки (3→∞), портфоліо (5→∞), розумні ціни (тріал→∞) — зняття Starter-капів

**Прибрано (обман клієнта):**
- Доставка Новою Поштою — НЕ реалізовано → задача `M-SHOP-05`
- CSV-експорт — enforcement не знайдено → задача `M-CLI-07`

## Ціль
Чесний, звірений з кодом перелік фіч у ЄДИНОМУ джерелі + Pro візуально домінантний.
**Позиціонування Pro:** «Усе зі Starter, без лімітів» + справжні розблокування. Домінування Pro — ДИЗАЙНОМ (accent, розмір, бейдж), НЕ інфляцією списку (чесних 2x-фіч немає — Pro це «Starter без капів»).

### Фінальний перелік (humanizer-пройдено, без em dash, без фейків)
**Starter (Free):** До 40 записів на місяць · Публічна сторінка, онлайн-запис 24/7 і QR-код · CRM клієнтів і пряма переписка · Нагадування та Telegram-сповіщення · Програма лояльності · Мультипослугові записи за один візит · Розумні ціни (пробний ліміт до 1000 ₴) · Флеш-акції: до 5 на місяць · Розсилки: до 3 на місяць · Портфоліо: до 5 робіт · Базова аналітика за поточний місяць

**Pro:** Усе зі Starter, без лімітів · Необмежені записи · Публічна сторінка без мітки Bookit · Розумні ціни без ліміту доходу · Флеш-акції та розсилки без ліміту · Необмежене портфоліо · Розширена аналітика: історія 6 місяців, прогноз і порівняння періодів · Магазин товарів на сторінці · Усі стилі сторіс без розмиття · Кастомна тема оформлення · Пріоритетна підтримка

**Studio:** Усе з Pro для кожного майстра · Управління командою · Спільна аналітика студії · Власний брендинг · Виділений менеджер

## Файли, які чіпаю
- `bookit/src/lib/constants/tierFeatures.ts` — НОВИЙ. Єдине джерело: `STARTER_FEATURES`, `PRO_FEATURES`, `STUDIO_FEATURES`. Обидві сторінки імпортують → кінець розбіжності 9-vs-13.
- `BillingPage.tsx` — імпорт констант; hero-Pro рендерить новий `PRO_FEATURES`; рядки Starter/Studio → розкривні (tap-to-expand accordion зі списком фіч). Studio-рядок: «Хочу в бету» лишається sibling-кнопкою (не кнопка-в-кнопці, патерн M-REVW-02).
- `LandingPricing.tsx` — імпорт констант; grid `items-stretch → items-start` (Pro природно вищий); бейдж «Рекомендуємо» на Pro; фічі лишаються видимими (без accordion — антиконверсійно на лендингу).

## Ризики / що може зламатись
- `submitBetaRequest` / cancel-модалка / beta-Sheet / actions — НЕ чіпати (лише перелік + layout).
- Accordion height-анімація в headless — статичні перевірки; `AnimatePresence` + `aria-expanded`.
- Landing `items-start` може розсинхронити CTA-кнопки по низу карток — перевірити вирівнювання (CTA не мусить «плавати»; за потреби `mt-auto`).
- a11y: розкривні рядки billing — `aria-expanded`, `type="button"`, touch ≥44px.

## Acceptance criteria
- [ ] TSC: 0 | Build: clean
- [ ] Нуль фіч, які не підтверджені кодом (нуль обману)
- [ ] Billing: рядки Starter/Studio розкриваються, показують звірений перелік
- [ ] Landing: Pro візуально домінує (вищий + бейдж), фічі видимі
- [ ] Обидві сторінки читають один `tierFeatures.ts`
- [ ] Рендер власними очима (Playwright): billing розкриті/згорнуті + landing desktop/mobile
- [ ] a11y MCP на нових інтерактивних елементах

## Відкриті питання до тебе
Немає — tier-карту й позиціонування підтверджено в діалозі. Чекаю APPROVE на брифі.

---

## [Заповнюється після DONE]
**Root cause / рішення:** Задача-COPY переросла в аудит чесності. Ключове відкриття — маркетинговий копі заявляв Pro-ексклюзивом фічі, доступні Starter (мультипослуги, лояльність, розумні ціни-тріал, T-chat, QR), і заявляв неіснуючі (Нова Пошта, CSV). Рішення: єдине джерело `tierFeatures.ts`, звірене з enforcement у коді; Pro-домінування дизайном (бо чесних 2x-фіч немає, Pro=«Starter без лімітів»); фейки винесено в беклог (M-SHOP-05, M-CLI-07).
**Commit:** `b668dd55` (код). Docs-коміт окремо.
**Що винесено в mempalace:** аудит tier-гейтів як обов'язковий крок перед будь-яким маркетинговим копі про фічі (звіряй enforcement, не PLANS-масиви); Pro продають ЛІМІТИ не фічі (multi-perspective); патерн єдиного джерела фіч між публічною і auth-сторінками.
