# Sprint-05 Transition Prompt

> **ONE TASK = ONE SESSION** — залізне правило.
> Копіюй і вставляй на початку КОЖНОЇ нової сесії Sprint-05.

---

```
Привіт. Продовжуємо Sprint-05 BookIT — загальний беклог (Зона Майстра + Клієнтська Зона).

═══ ОБОВ'ЯЗКОВИЙ STARTUP (виконай ДО будь-чого іншого) ═══
1. mempalace_status
2. Read XDEV/MAPS/SYSTEM_MAP.md (останні 50 рядків, offset mode)
3. Read XDEV/PLANS/SPRINT-05-BACKLOG/TRACKER.md — знайди ▶ NEXT рядок
4. Відповісти: "STARTUP OK: Palace [N] drawers | Next: [ID] — [назва]"

═══ ПОТОЧНИЙ СТАН ═══
Прогрес: 62/86 ✅ · 3 ↩️ (M-DASH-11, M-MKT-01, M-MKT-02 скасовано) | Sprint-05 IN PROGRESS · ФАЗА 3 закрита (ANALYTICS 7/7 ✅) · ФАЗА 4: усі Settings M-SET-01..05 ✅ · M-BILL-01 ✅ (задеплоєно) · M-BILL-02 ✅ (НЕ задеплоєно). Total 84→86 (+M-SHOP-05 Нова Пошта, +M-CLI-07 CSV — чесні борги з M-BILL-02).

▶ НАСТУПНА ДІЯ: `M-PORT-01` — Портфоліо: стандартизувати розмір карток (тип REDESIGN, Sonnet). По порядку трекера у Фазі 4.
  Ціль: PortfolioPage.tsx (/dashboard/portfolio) — єдиний розмір карток робіт. Скіли impeccable (layout) + design-taste-frontend.
  Перед стартом — Task Gate: mempalace_search → скрін + читати живий код → пре-код ритуал (brainstorming→impeccable craft→grill) → бриф → ОК founder → код.
  Деталі: HANDOFF.md ▶ NEXT.

Нотатки 2026-07-01 (M-BILL-02, commit b668dd55, founder закрив задачу, НЕ задеплоєно): чесний перелік фіч тарифів + Pro домінантний. Задача-COPY переросла в аудит проти обману клієнта. 🔴 ГОЛОВНИЙ УРОК: перед будь-яким маркетинговим копі про фічі — АУДИТ tier-гейтів проти enforcement у коді, НЕ довіряй PLANS-масивам. Простежено ~13 фіч: доступне Starter (НЕ Pro-ексклюзив) — кілька послуг/лояльність/розумні ціни(тріал 1000₴ createBooking:270)/T-chat/QR; реальні Pro-гейти — безліміт записів(createBooking:179)/вотермарка(PublicMasterPage:1077)/аналітика(AnalyticsPage:111)/магазин(:792)/тема(TechnicalIsland:49)/сторіс(useStoryEditor:157). Прибрано ФЕЙКИ: Нова Пошта (не реалізовано→M-SHOP-05) + CSV (enforcement не знайдено→M-CLI-07). Єдине джерело lib/constants/tierFeatures.ts→BillingPage+LandingPricing (кінець розбіжності 9-vs-13). Billing: accordion-рядки Starter/Studio. Landing: Pro домінантний ДИЗАЙНОМ (бейдж+scale, бо чесних 2x-фіч нема — Pro=«Starter без лімітів»); теплий підрядок «Коли ти ростеш, Pro росте з тобою.»; Studio ожила («Формуємо першу групу студій»+клікабельний пункт беты «Ваші ідеї та потреби формують продукт»); сторіс як унікальна фіча «Сторіс для Instagram із вільних вікон». Скіли: multi-perspective-analysis (Pro продають ЛІМІТИ не фічі; найсильніший апсел у ТОЧЦІ БОЛЮ — кап 40 записів/магазин — НЕ на /billing) + humanizer + design-taste-frontend. Рендер власними очима landing (3 ітерації). Billing accordion піксельно НЕ бачив (за auth) — текст=те саме джерело. TSC:0 Build:clean. Уроки: (1) звіряй enforcement перед копі про фічі — PLANS-масив ≠ реальний гейт; (2) щедрий Starter — PLG-двигун, не баг; Pro=зняті ліміти; (3) не роздувай Pro для «2x» — це поверне обман, домінуй дизайном.

Нотатки 2026-07-01 (M-BILL-01, commit 441e1e1f, deploy dpl_5RnDcCrnSqmQAvGd6dRn39Ed9RNd READY, founder QA пройдено): повний редизайн сторінки тарифів BillingPage за Законом темного блоку. State-aware герой: Starter→офер Pro (serif-ціна 700₴ clamp Cormorant + УСІ 9 можливостей 2 колонки + біла інвертована CTA + trust Monobank), Pro/Studio→керування (active «Наступне списання X» / canceled «Діє до X, автопродовження вимкнено»+CTA «Відновити» / no-card «Прив'язати картку»; чіп статусу картки за subscription.token, дедуп із рядком). Верхній ряд асиметрія col-8 герой (slate #0F172A + аврора) + col-4 Monobank-панель (плаский чорний #0A0A0A, ОФІЦІЙНИЙ monobank SVG public/monobank-logo.svg з Wikimedia, білий через [filter:brightness(0)_invert(1)], інфо-довіра SSL/картка/керуй, тиха бренд-мітка картки). Плани = 1 герой + 2 тихі диференційовані hairline-рядки (Starter «Ваш план» / Studio «Скоро»+beta-лінк), НЕ 3 картки. Прибрано фейк-тумблер «Спосіб оплати» (1 кнопка+мертвий provider-стейт) + емодзі 🍋. Бекенд/actions/searchParams/cancel-модалка/beta-Sheet НЕ чіпано. 🔴 Два темні блоки диференційовано МАТЕРІАЛОМ (§5: editorial slate+аврора+serif vs плаский чорний sans-чіп), не темрявою. a11y MCP (trust white/50 на slate=5.23, emerald-chip=11.71), humanizer (em dash прибрано). Рендер власними очима 8 скрінів (starter/pro/canceled/nocard × desk/mob) через мок-прев'ю /bill-preview (тимч. export MasterContext + Playwright, видалено). 2 ітерації founder: усі-9-фіч (не 4) + офіційний SVG (не текст). TSC:0 Build:clean. Уроки: (1) фейк-тумблер 1-опція+мертвий стейт=прибрати; (2) grayscale моно-лого→brightness(0) invert(1)=біле на темному; (3) SVG в next/image блок→плаский img+eslint-disable; (4) прев'ю віджета з MasterContext=тимч. export сирого контексту+мок-Provider; (5) обережно Edit const X→export const X якщо попередній не ревертнутий (export export=краш).

🚀 ЗАДЕПЛОЄНО НА ПРОД 2026-07-01 (vercel --prod CLI з bookit/, deploy bookit-cta5akidq… READY): усе Sprint-05 до M-SET-04 + ad-hoc фікси. 🔴 ВАЖЛИВІ ІНФРА-ФАКТИ: (1) Live URL = bookit-winston1234564757s-projects.vercel.app — саме ним користується founder; bookit.com.ua НЕ прив'язаний до Vercel-проєкту (domains ls=0, Cloudflare→інший origin→404), НЕ чіпати. (2) git РАНІШЕ НІКОЛИ не пушився — 78 комітів лежали локально; тепер origin/main синхрон, пушити після кожного коміту. (3) Деплой = vercel --prod CLI (НЕ git-integration). (4) PWA SW кешує → після деплою founder робить ПОВНИЙ реоупен.

Ad-hoc фікси 2026-07-01 (репорт founder): навбар-стрибок cf801b34 (прибрано framer layout з fixed-смуги MobileHub, ⚠️iOS-транзієнт); локація+карта 6daa465b (Google білінг=інфра founder, лишили Google+редизайн+premium MapFallback+deep-link); 🔴 DATE/TIME ІНПУТИ overflow фінал 8694b2fe→3b65bbd4→6631ca3a: native date/time input ІГНОРУЄ власний max-width і min-width:0 (розтягується до інтринсік) → max-w на самому інпуті НЕ діє → фікс = обгортка-div max-w-[13rem] + input w-full (div поважає max-width). Переюзний урок для будь-яких native date/time у вузьких блоках.

Нотатки 2026-07-01 (M-SET-04, commit 207f1955, founder QA пройдено, очікує деплою): редизайн секції «Вихідні та відпустки» у /dashboard/settings — концепт «Коли мене немає», широка секція власним рядом col-10. ОБ'ЄДНАНО з тижневими днями (рішення founder «чистіше»): тижнева банда-герой full-width Пн–Нд (тап=робочий/вихідний, пише schedule.is_working) + hero «N/7 робочих днів» + разові відсутності (відпустка/вихідний/короткий день: список col-7 + форма col-5). 🔴 Керування «які дні» ПОВНІСТЮ переїхало сюди — з ScheduleWidget прибрано тумблер дня (лишились години/буфер/перерви + натяк «вихідні — у Вихідні та відпустки»; off-день read-only «Вихідний»). Єдиний стан useSettingsForm → без конфлікту. Split VacationManager(контейнер+useTimeOff+onScheduleChange, будує onToggleDay) / VacationManagerView(презентація props) → рендер власними очима devpreview. SettingsPage грід: Vacations col-3→col-10, ребаланс Retention col-4 + Segments col-6, обгортку-хедер прибрано, CalendarOff import видалено. a11y muted-foreground/xx → text-text-sub. Верифіковано власними очима desktop+mobile повний+empty. TSC:0 Build:clean. Уроки: (1) week ліворуч+one-off праворуч дало порожню ліву колонку → week=повноширинна банда-герой зверху, one-off 2 колонки під; (2) «включити тижневі дні»+«чистіше» = single-source useSettingsForm з одним UI on/off (тут), другий (ScheduleWidget) лише години — розділення відповідальності; (3) split container/view+devpreview = рендер віджета що вимагає MasterProvider без auth.

Нотатки 2026-07-01 (M-SET-05, commit c8b4d9ff, BUGFIX поза чергою на репорт founder, founder QA пройдено): ScheduleWidget смарт-статус. БАГ: о 8:50 (день не почався, старт 09:00) казав «Зараз перерва». Root cause: isWorkingNow бінарний (в робочих годинах/ні), усе інше (до відкриття/після закриття/вихідний) → «Зараз перерва»; breaks[] взагалі не перевірялись (реальна перерва казала б «працюю»); now=new Date() раз при рендері без тику → застрягав до reload (скрін о 09:03 показував обрахунок 8:50). FIX: 5 станів LiveStatus (working/break/before «Відкриття о HH:mm»/after «Робочий день завершено»/off «Сьогодні вихідний») у useMemo за хвилинами; breaks[] читаються; useEffect setInterval(30с) setNow(getNow()) → тик оновлює на межах без reload; спільні getNow()/toMins() (як useBusyness); прибрано format+isWorkingNow. STATUS_UI record: working=success chip/#0D6B2F label, break=warning chip/#92400E, before/after/off=neutral bg-secondary/text-text-sub. Заповненість по днях звірено (useBusyness dayOccupancy forward 30д по DOW) — коректна, 0% бо нема майбутніх записів (не баг). a11y #0D6B2F(5.16)/#92400E(5.56)/text-text-sub(5.94). Верифіковано власними очима ВСІ 5 станів через time-travel cookie next-public-debug-now (devpreview/schedule + Playwright). TSC:0 Build:clean. Уроки: (1) бінарний «працюю/перерва» на часовому статусі — пастка: не-робочий-час ≠ перерва (до-відкриття/після-закриття/вихідний окремі); (2) статус на new Date() при рендері застрягає — потрібен setInterval-тик; (3) getNow() cookie next-public-debug-now = time-travel для верифікації часових станів власними очима; (4) перевіряй що логіка консультує ВСІ джерела (breaks[] ігнорувались).

Нотатки 2026-07-01 (M-SET-03, commit 721d9182, founder QA пройдено, очікує деплою за командою): SmartAdvisor («BookIT Assistant» у /dashboard/settings) — концепт «Наступний крок». Пасивні поради → КЛІКАБЕЛЬНІ: тап будь-якої → jumpToSection(id) = getElementById→scrollIntoView({block:'center'}) + .advisor-highlight (CSS keyframe advisor-pulse box-shadow ring var(--accent) 1.7с, reflow-reset void offsetWidth для повторного кліку, reduced-motion aware). Якорі: фото→#hero, опис→#identity, IG→#technical, спеціалізація→#categories (+id у SettingsPage). Візуал: 🔴 БАННИЙ hero-metric (7xl % score) ДЕМОТОВАНО → чесний «Заповненість профілю · N/4» + слім-смуга h-1.5; ГЕРОЙ = найважливіша наступна дія (акцент-тінт блок warning/[0.11] / accent/[0.09] / success + eyebrow «Наступний крок» + великий title + опис + афорданс «Перейти до розділу ›» ChevronRight, весь блок=button); решта поради = компактні клікабельні рядки з chevron; done 4/4 = святковий CheckCircle-блок. 🔴 ФЕЙК ПРИБРАНО: пояснення стверджувало «% заповненості→позиція в каталозі»; звірка explore/page.tsx = сорт .order('rating_count',desc)+персоналізація категорій, заповненість НЕ впливає → переписано чесно (заповненість→довіра/конверсія; позиція від відгуків/записів). a11y: дрібний text-text-mute (2.76 бан) → text-text-sub (#475569=5.94 verified a11y MCP) усюди. Тип виявився REDESIGN+interaction (не COPY — звірка живого коду). Логіку обрахунку tips/score НЕ чіпано. TSC:0 Build:clean. Верифіковано власними очима (devpreview/advisor + Playwright: empty/partial/done). Уроки: (1) «assistant активний» = навігаційні поради (scroll+highlight), не косметика; (2) 7xl %-score = банний hero-metric template → демотуй у чесний N/4, герой=дія; (3) звіряй сорт каталогу (rating_count не completeness) перед обіцянками впливу; (4) .advisor-highlight reflow-reset щоб keyframe перезапустився при повторному тапі.

Нотатки 2026-07-01 (M-SET-02, commit ded4332d, founder QA пройдено «це воно», очікує деплою за командою): ProfileHero редизайн (блок інфо профілю в /dashboard/settings) — концепт «Опублікована обкладинка». Плаский нижній стос (sans-ім'я text-xl + bio-рамка + 2 конкуруючі дрібні дії share+«Переглянути») → асиметрія (Закон темного блоку на СВІТЛІЙ темі): фото-герой на всю картку + editorial-скрім + ІМ'Я Cormorant serif (heading-serif) = єдина домінанта bottom-left + тихий підвал (рейтинг text-warning+metric-value / N відгуків text-text-sub) + ОДНА домінантна дія full-width bg-foreground «Переглянути сторінку» (copy = вторинний size-11 icon). 🔴 Serif-ім'я на СВІТЛОМУ фото втрачало контраст → скрім from-black/75→from-black/85 (h-62%) + [text-shadow:0_1px_14px_rgba(0,0,0,.5)] (верифіковано на екстремально яскравому фото). 🔴 Empty-стан вимитий (біле ім'я на світлому placeholder from-secondary to-muted) → from-slate-700 to-slate-900 + камера/нудж text-white/75 («обкладинка в очікуванні фото»). a11y: #D4935A→text-warning (#B45309), muted-foreground/60→text-text-sub (5.88). Bio прибрано (живе в Identity), −1 prop у SettingsPage. Identity-дубль НЕ чіпано (рішення founder). Верифіковано власними очима (devpreview/profilehero + Playwright: фото/empty/mobile). TSC:0 Build:clean. Уроки: (1) текст-над-фото — скрім САМ не гарантує читабельність на світлому фото → скрім + text-shadow разом (не пере-затемнює гарні фото); (2) empty-стан фото-hero має власну драму (темний placeholder), не вимитий світлий; (3) picsum не вантажиться в headless — бери public/ асет для верифікації happy-path.

Ревізія лейауту 2026-06-30 (M-ANL-07, commit 482784eb, на запит founder «новий лейаут для зростання»): height parity у табі «Зростання» — `grid items-start → items-stretch`; парні картки тепер однакової висоти (LTV+winback col-7 ↔ Канали col-5 розтягнуто `flex-1 justify-between`; нижній ряд 5/7 → **6/6** «Спрацювання» ↔ «Що бронюють», вміст по центру `flex-1 justify-center`). Прибрано дублювання концентрації: герой показує «Постійних клієнтів» замість «Топ-20% частка» (% лишився лише в LTV-картці). Верифіковано власними очима (devpreview + Playwright). TSC:0 Build:clean.

Нотатки 2026-06-30 (M-ANL-07, commit 987b3d67, очікує візуального QA founder — Analytics ПОВНІСТЮ закрита 7/7): таб «Зростання» (`growth`) editorial-redesign — концепт «двигуни росту». 8 рівних карток (BentoSecondary 6 + GrowthLists + cohort) → асиметрія. ТЕМНИЙ герой (alternating) = додатковий дохід від розумних цін +₴N домінанта (metric-value) + врятовані слоти + by-numbers (топ-20% частка / флеш заброньовано / розсилки→записи); клік→upliftDetail (розбивка правил + cta «Налаштувати ціни»). Fallback uplift=0 → LTV-концентрація X% + нудж «Увімкнути розумні ціни»→/dashboard/revenue. LTV Парето featured (col-7): концентрація% + гістограма СТАТИЧНИХ смуг + winback герой+ранг top-4 клік→профіль. Канали залучення (col-5): флеш/розсилки/реферали/постійні клієнти диференційованими рядками. Спрацювання розумних цін (col-5) + Що бронюють разом (col-7) смугами. Когорти (col-12, reuse CohortHeatmap). 🔴 ЧЕСНІ ЛЕЙБЛИ: «Постійні клієнти»+«N візитів сумарно» замість «Клуб лояльності/нараховано балів» (хук рахує total_visits, не бали — 5-й фейк-лейбл виправлено). 🔴 STATIC bars: LtvHistogram+ServicePairingMatrix мали framer animate-width (зникли б у headless) → переписано власними static-смугами. Консолідовано GrowthTab(props)+GrowthTabView; ВИДАЛЕНО 8 мертвих файлів (BentoSecondary, GrowthLists, DynamicPricingUplift, FlashDealsCard, BroadcastEngagement, LtvConcentration, ServicePairing, LtvHistogram). GoalProgress прибрано зі Зростання (founder; компонент лишено — є тести); ServicePairingMatrix лишено (тип ServicePair). Дані: growth з page-level extras + useAnalyticsMarketing пропсами (НЕ re-fetch); hasData=summary.bookings>0. Pro-gate value-екран. a11y #92400E(5.56). TSC:0 Build:clean humanizer✓. Верифіковано власними очима (full/noUplift/mobile). Уроки: (1) framer animate-width зникає в headless (вчетверте — Growth bars); (2) видаляєш render-only компоненти → перевір чи їх тип ще імпортується (ServicePairingMatrix лишив заради ServicePair); (3) «нараховано балів»=total_visits — звіряй що метрика РЕАЛЬНО рахує перед лейблом.

Нотатки 2026-06-30 (M-ANL-06, commit f21d84fd, очікує візуального QA founder): таб «Поведінка» (`behavior`) editorial-redesign — концепт «ритм бізнесу». 4 рівні стопки-блоки → асиметрія. СВІТЛИЙ герой (alternating) = теплова карта завантаження ВЕЛИКА (col-7, HeatmapGrid reuse) + пік-інсайт домінантою (col-5: найзавантаженіший день serif + occupancy% о годині + by-numbers, клік→пояснення; peak=heatmap.reduce max occupancy_pct). No-Show=герой секції (col-7): рейти неявок(destructive)/скасувань(#92400E) + журнал інцидентів top-8 клік→incidentDetail→OverviewDetailSheet (що сталося/дата/телефон/причина + cta «Профіль клієнта»→/dashboard/clients?clientPhone). Lead time=врізка (col-5): сер.час serif + розподіл 5 бакетів смугами СТАТИЧНОЇ ширини (старий LeadTimeTab мав framer animate width — зникав би в headless). Vacation=тиха повноширинна врізка (col-12, лише offSegmentsCount>0): чесний недоотриманий дохід + сер.дохід/день + лінк «Налаштувати вихідні»→/dashboard/settings#vacations. 🔴 ФІКС ФЕЙКУ: прибрано VacationTab «Рятувальник відпустки» ЦІЛКОМ — кнопка «Оптимізувати розклад» (лише тост) + «Врятуйте ₴X» (lost*0.4 вигадка). Консолідовано 3 під-таби (NoShowTab/LeadTimeTab/VacationTab)+інлайн heatmap у BehaviorTab(fetch 3 хуки+Pro-gate)+BehaviorTabView(презентація); СТАРІ 3 ФАЙЛИ ВИДАЛЕНО; AnalyticsPage прибрано імпорт HeatmapGrid. Pro-gate value-екран (раніше Starter бачив порожнечу). a11y text-muted-foreground/60→text-text-sub, #92400E(5.56) дрібний warning, #B91C1C(5.08). Дані: bookings.total_price=ГРИВНІ, useVacationImpact ×100→копійки (grn=/100). TSC:0 Build:clean humanizer✓. Верифіковано власними очима (full/clean/mobile). Уроки: (1) framer animate-width зникає в headless (вже втретє — Behavior LeadTime); (2) консолідація стопки-під-табів в один Tab+View покращує асиметрію і дає прев'ю; (3) видалив прев'ю-роут→build падає на стале .next/dev/types→rm -rf .next/dev/types .next/types+ребілд.

Нотатки 2026-06-30 (M-ANL-05, commit bbd45815, очікує візуального QA founder): таб «Фінанси» (`finances`) editorial-redesign — концепт «P&L звіт». ТЕМНИЙ герой (alternating) = чистий прибуток домінантою (`metric-value` tabular + ₴ окремо; loss→rose-300) + Δ до минулого періоду (Pro) + by-numbers виручка/витрати/чиста маржа. Δ: `FinancesTab` викликає `useAnalyticsExtras` ДВІЧІ (поточний + `previousWindow(start,end)` локальний helper як у useSourceAttribution); deltaNet=(curr−prev)/|prev|·100, guard prev=0/null. ЧЕСНИЙ КАСКАД руху грошей (світла секція): реальний потік виручка послуг+товарів→subtotal→мінус собівартість/знижки/опекс→прибуток, hairline-смуги СТАТИЧНОЇ ширини (`style width` НЕ framer animate — зникає в headless, урок!), нульові рядки відфільтровано. Маржинальність: герой ризику = найтонша маржа (<40% warning+AlertTriangle / інакше neutral) + ранг-список; клік→`serviceMarginDetail` (виручка/собівартість/прибуток/записів+cta). Рекомендацію «+10%» прибрано→нейтральний лінк «Переглянути ціну». 🔴 4-Й ФЕЙК (не в брифі, знайдено в коді): `WaterfallChart` вигадував `noShowLoss=servicesRevenue*0.05` і роздував фейковий «Загальний вал» → ФАЙЛ ВИДАЛЕНО (єдиний споживач). Empty Pro→skeleton без цифр (прибрано `displayFin`). Розділено `FinancesTab`(fetch+Δ)+`FinancesTabView`(презентація); дані `FinanceAnalytics` усе в КОПІЙКАХ (grn=/100). a11y #0D6B2F(5.21)/#B91C1C(5.08)/emerald-300/rose-300. Верифіковано власними очима (profit/healthy/loss/mobile). TSC:0 Build:clean humanizer✓. Уроки: (1) framer animate-width зникає в headless — статичний style width; (2) звіряй живий код на фейки навіть поза задокументованим списком (4-й фейк WaterfallChart); (3) Δ через 2-й виклик хука з previousWindow — переюзний патерн.

Нотатки 2026-06-30 (M-ANL-04, commit 924105d6, очікує візуального QA founder): таб «Склад» (`stock`) editorial-redesign — концепт «склад під контролем». СВІТЛИЙ герой (alternating) = найтерміновіший розхідник (`sorted[0]`), днів до закінчення великим числом. 🔴 УРОК: число через `metric-value` (tabular sans), а НЕ `heading-serif` — Cormorant рендерив "18" як "I8" (oldstyle-цифри), зловлено на власному рендері. Світлофор-tone `severityOf`: critical ≤3 / warning ≤7 / normal; `TONE[sev]`={dot,eyebrow,daysBig,label}. by-numbers: запас/витрата-30д/прогноз; клік→OverviewDetailSheet (запас/витрата/потрібно на 14д/собівартість + cta «Поповнити запас»→/dashboard/products). Col-5 світлофор Критичні/Увага/У нормі + СПИСОК ПОКУПОК (count до закупівлі + Сформувати/Копіювати з copy-to-clipboard; empty «Усе в нормі, закуповувати поки нічого»). Col-12 усі розхідники ранг-списком за терміновістю + status-dot + кольоровий days-лейбл. 🔴 ФІКС ФЕЙКУ: Pro empty-state → skeleton-силует (сірі смуги БЕЗ цифр) + teaser; прибрано захардкоджені dummy-products під блюром. Розділено StockTab(fetch)+StockTabView(презентація). 🔴 ЛАТЕНТНИЙ БАГ M-ANL-02 виправлено в цьому коміті: SourceTab DeltaChip-down `text-error`→`text-destructive` (`--color-error` не існує в Tailwind v4, мапінг `--color-destructive: var(--error)`; раніше рендерилось темним замість червоного). a11y: critical #B91C1C, warning eyebrow #92400E (5.56), normal eyebrow #0D6B2F (5.21) на periwinkle #DCE3FF. Верифіковано власними очима (Playwright headless crit/ok/mobile). TSC:0 Build:clean humanizer✓.

═══ M-ANL-01 «Огляд» — ЗРОБЛЕНО (commit 2c48474f, ЗАДЕПЛОЄНО, чекає візуального QA founder) ═══
**⭐ НАРОДЖЕНО ЗАКОН: Принцип темного блоку** (WORKFLOW.md §, memory feedback_dark_block_principle.md, MemPalace). Founder відкинув косметику світлих карток («дитсадок»); корінь = МИСЛЕННЯ: проектувати з концепту з нуля + асиметрична ієрархія (герой+решток) в КОЖНІЙ секції, не ретрофіт. Маркер провалу = рівномірність. Воркфлоу: рендер локально власними очима (Playwright headless + мок-прев'ю-роут поза auth) ДО показу founder.

**Огляд = editorial-дашборд:**
- Нові файли: `sections/OverviewBriefing.tsx` (темна editorial-обкладинка: виручка-герой serif + Δ Pro + інсайт-колонка + by-numbers стрічка), `OverviewTab.tsx` (+`SectionHeading` спільний), `FeaturedServices/Products/Clients.tsx` (герой+ранг-список), `OverviewDetailSheet.tsx` (адаптивний Sheet деталей, payload hero/rows/note/cta), `AnalyticsActivation.tsx` (empty-state новачка), `ClientSheetById.tsx` + `lib/supabase/hooks/useAnalyticsMarketing.ts` (декомпозиція 988-рядкового оркестратора).
- `charts/RevenueLineChart.tsx` — ПЕРЕПИСАНО на **recharts** (ComposedChart+градієнт+пунктир прогнозу+кастомний Frost-тултіп). Виправлено «сплюснутість» (саморобний SVG preserveAspectRatio=none). 🔴 `isAnimationActive={false}` на Area ОБОВ'ЯЗКОВО — інакше зникає в headless/фон-табі.
- `useAnalytics.ts` — адитивна добавка `bento.revenue`/`bento.bookings` Δ (дзеркало avgCheck, нуль нових запитів).
- Клікабельність: виручка→розбивка категорій, by-numbers→пояснення, послуга/товар→розширений огляд, клієнт→ClientDetailSheet. Усе через реальні дані (нуль фейку).
- Гейти: TSC 0 · Build clean (2.7min) · a11y (slate-контраст 6.09–11.71) · humanizer. Прев'ю-роут видалено.
- ✅ `sections/ServiceRow.tsx` (мертвий, замінено FeaturedServices) — видалено в коміті.
- 🔴 У ANALYTICS_TABS_REDESIGN.md задокументовано 2 ФЕЙКИ для виправлення в табах: VacationTab «Оптимізувати розклад» (порожня кнопка+тост), FinancesTab/StockTab dummy-числа під блюром.

═══ M-ANL-01 (стара версія заголовка, для довідки) ═══
Тип REDESIGN Тір 2 · скіли: impeccable (розширений) + design-taste-frontend · фінал a11y+humanizer · Opus · P1. Бриф: BRIEFS/M-ANL-01.md.
Нотатки 2026-06-29 (M-REVW-01 + M-REVW-02, commit 206fee3f, deploy dpl_D2mLionYHEtGYwAJaw855VRM7TxQ READY, очікує візуального QA founder): преміум-редизайн сторінки відгуків майстра (/dashboard/reviews). Hero-зріз (середній рейтинг serif avgAll.toFixed(1) + зірки + «N відгуків» pluralUk) замінив 3 однакові stat-картки; тиха стрічка Публічні·Приховані без коробок (de-template патерн M-MKT-06). ГІСТОГРАМА-ЯК-ФІЛЬТР (головне дизайн-рішення, founder підтвердив): розподіл 5★→1★ зі смугами-часткою (count/maxBar над УСІМА відгуками = справжній розподіл), кожен рядок = button aria-pressed → toggle у ratingFilter Set<number> (multi-select), disabled на count=0; один елемент несе і розподіл, і фільтр замість окремого ряду зіркових чіпів. Контролі: пігулки публікації + тогл «Лише з коментарем» + native select сорту (newest/oldest/highest/lowest, ChevronDown) + «Скинути»; усі AND в одному visible useMemo (tie-break за датою для рейтинг-сортів). M-REVW-02 клікабельні картки: full-card underlay button absolute inset-0 z-0 → setDetail; контент relative z-10 pointer-events-none; publish-тогл виняток pointer-events-auto relative z-10 (a11y: не кнопка-в-кнопці, патерн M-SHOP-02/M-SVC-03). ReviewDetailSheet (новий) = адаптивний Sheet reuse (Dialog desktop/vaul mobile), повний коментар whitespace-pre-line без обрізки + дія публікації. A11Y MCP зловив діру: Frost muted-foreground (=text-tertiary rgba(15,23,42,0.45)≈#8187A0) на periwinkle = 2.76:1 провал малого тексту → весь дрібний інфо-текст на text-text-sub (=--text-secondary #475569 = 5.88:1 ✓, 130 наявних ужитків); зірки/смуги amber #B45309 3.90 (графіка 3:1 ✓). У Frost text-primary=--accent=#0F172A (не #789A99 старіших тем) — лінк «Скинути» безпечний. Тур useTour('reviews')+AnchoredTooltip anchor + Pro-нудж збережені. useReviews не чіпано (сорт/фільтр на клієнті). TSC:0 Build:clean. Уроки: (1) гістограма-як-фільтр = два завдання (розподіл + фільтр за балом) в одному елементі; (2) full-card underlay + pointer-events-none контент + sibling-виняток на вторинну дію = клікабельна картка без вкладеної кнопки; (3) Frost muted-foreground (2.76) провалює дрібний текст на periwinkle — бери text-text-sub (#475569); (4) звіряй живий компонент перед оцінкою скоупу (сторінка вже мала фільтр публікації, бракувало сорту + зіркового фільтра).
Нотатки 2026-06-29 (M-MKT-05 + M-MKT-06, commit 8bd25847, задеплоєно на прод, founder QA пройдено «все чудово»): inline per-client деталі розсилок через BroadcastDetailSheet (vaul-оверлей) замість редіректу на /marketing/[id] (роут + BroadcastDetailPage ВИДАЛЕНО; підключено раніше мертвий Sheet — 3 рядки стану). Преміальний редизайн: легасі-peach-хекси→Frost-токени в усіх 3 компонентах (BroadcastHistory/DetailSheet/Tab). De-nest аналітики: nested cards + 7 однакових StatCard (подвійний бан impeccable) → 2 hero-outcome (Записалось/Конверсія) + тиха роздільна стрічка вторинних метрик. Канальна палітра Frost (App=slate/Push=success/Telegram=#2563EB навмисно/SMS=warning), іконки 3.89–4.01 на periwinkle (3:1 ✓, сенс дублює форма check/x). tsc 0 (post-build — .next/types валідатор ламав tsc на видалений роут, build регенерує), build clean, a11y MCP ✓. Контртеза: назва «статистика inline» вводила в оману — статистика вже була inline; реальна діра M-MKT-05 = один редірект, основний обсяг = M-MKT-06. Уроки: (1) звіряй живий компонент перед оцінкою обсягу; (2) «inline без редіректу» = підключити готовий мертвий Sheet; (3) видалив роут → перевіряй tsc ПІСЛЯ build (стале .next/types).
Нотатки 2026-06-29 (M-MKT-04 + M-MKT-03, commit 7b2e72d8, очікує візуального QA founder, НЕ задеплоєно): StoryGenerator з одного скролу → покроковий проф-едітор (5 кроків: Тип→Контент→Вигляд→Стиль→Готово) із завжди-pinned live-прев'ю (mobile зверху / desktop sticky праворуч). Рішення founder: ПОВНИЙ рерайт шелла, АЛЕ reuse рендер/експорт/дата-фундаменту (StoryCanvas/storyExport/useStoryData) — переписувати їх = ризик фіделіті експорту 1080×1920 без UX-вигоди (я наполіг, founder погодив звуження). Архітектура: `story/useStoryEditor.ts` (хук-мозок: весь стейт+преміум blur-gating 10с/3с+навігація+взаємовиключні фон-джерела), `story/storySteps.ts` (STEPS+isStepComplete advisory, НЕ хард-блок), UI-панелі `story/steps/*`, `StoryPreview`/`StepNav`. Друга вимога founder (шаблони): TEXT_TEMPLATES (текст-пресети по-режимах, крок Контент) + BG_GRADIENTS (6 CSS-градієнтів, крок Вигляд) + STOCK_PHOTOS (🔴 БЛОКЕР: порожній слот — потребує реальних файлів у public/story-bg/, секція схована; founder має дати ассети). Canvas: адитивний bgGradientCss-шар (єдина зміна reused-canvas). M-MKT-03: палітр 6→10 (+terracotta/lavender/forest/champagne, contrast ≥10.8 AAA). TDD: 32 unit-тести (10 файлів) — логіка (палітри/step-модель/хук-гейтинг/навігація) + render-behavior; пікселі НЕ TDD-или свідомо. Lint: react-hooks/set-state-in-effect — pre-existing repo baseline (53 помилки/42 файли, НЕ enforced gate); я звів свій файл 7→4 деривацією дефолтів. tsc 0, build clean. Експорт/TMA/преміум-гейтинг збережені 1:1. Уроки: (1) «повний рерайт» на launch-critical поверхні треба звужувати до шелла — reuse тестованого рендер/експорт-ядра; (2) фото-шаблони ≠ безкоштовні: стокові потребують ліцензованих ассетів, градієнти генеруються в коді миттєво; (3) взаємовиключність фону: вибір градієнта чистить фото-джерела і навпаки.
Нотатки 2026-06-28 (M-GROW-02, commit 31557c87, очікує візуального QA founder, НЕ задеплоєно): повний мердж master_partners+master_alliances → нова master_connections (bilateral, kind partner/alliance + role inviter/invitee). master_referrals (білінг) НЕ чіпано (рамка founder). Міграція 20260628000008 additive+reversible (старі таблиці лишаються, drop окремо після verify — БОРГ). Backfill alliance→2 рядки, dedup partner>alliance, row-count верифіковано. 7 споживачів перенацілено (partners.ts/referrals.ts write, getGrowthPageData/[slug]/wizard/AllianceMap read, 2 тести). UI: PartnersPage 2 секції→1 список мережі з origin-бейджами. 🔴 Латентний баг: trustedPartners на публічній сторінці був мертвий для анонів (стара RLS лише сторони) → mc_public_read оживляє. Security: write лише service_role, public-read non-PII opt-in, FK 23503 недоторк, advisor clean. a11y #3F5C5B 5.02. Уроки: (1) розвідка ДО брифа на HARD — «об'єднати X+Y» неоднозначне, виявилось 3 сутності+білінг-coupling; (2) bilateral модель уніфікує directional через role; (3) Supabase вузька RLS вбиває anon-читання — публічні фічі потребують явної anon-політики; (4) text-primary Frost #789A99 провалює малий текст → #3F5C5B+.
Нотатки 2026-06-28 (M-GROW-01, commit 3cf3deea, очікує візуального QA founder, НЕ задеплоєно — тримаємо до QA): сторінка лояльності з форми → панель керування. OverviewCard: тріада тап-сегментів у прогресі/готові/за крок (icon-чіп+число несе колір, не side-stripe; не hero-metric шаблон) → тап у /clients звужений + impact-смуга «₴ віддано · N разів» (30д forward-only, чесний empty). Картки програм progress-aware: двосегментна смуга reached/on_track + «X на шляху · Y готові», Y клікабельне. Info-банер ЛИШЕ в empty (3-сек правило). Бекенд forward-only: bookings.loyalty_label/amount (міг 20260628000006) + createBooking пише знижку лояльності (раніше викидав, одиниця=гривні); RPC get_loyalty_overview(pipeline all-time, total_visits) + get_loyalty_impact(30д) (міг 20260628000007), auth.uid без IDOR. Security: advisor зловив anon успадковує EXECUTE попри REVOKE public → REVOKE FROM anon явно (строго краще за M-REV-05; витоку не було, auth.uid=NULL→порожньо). A11y mcp: text-success Frost #16803C провалює малий текст на periwinkle (3.45-3.89) → малі лінки #0D6B2F 5.16 / чіп #0A5526 6.17. ClientsPage: ?loyaltyMin/loyaltyExact фільтр + dismissible чіп. ВІДОМА діра (документ.): total_visits тригериться лише для зареєстрованих — гостьові візити не входять, взято свідомо (консистентність /clients). Уроки: (1) DATA-розвідка ДО дизайну — знижка лояльності ніде не персистилась, impact неможливий без міграції → forward-only + чесний empty; (2) Supabase REVOKE public НЕ знімає anon EXECUTE, завжди REVOKE FROM anon на auth.uid-RPC; (3) text-success Frost провалює малий текст — бери #0D6B2F+.
Нотатки 2026-06-28 (M-REV-06, commit 3d7a11ef, очікує візуального QA founder): distill інфо-блоку ціноутворення (весь PricingHero). 3 тап-чіпи механіки (Стекінг/Max-30/Max+50) + карет-поповер → один тихий рядок-виноска під hairline, завжди видимий: «Правила складаються, але ціна тримається в межах від -30% до +50%». Прибрано INFO_CHIPS, activeChip, AnimatePresence поповер, caretLeft (−43 рядки). A11y-дірка зловлена grill-me ДО коду: muted-foreground (=text-tertiary) на periwinkle = 2.79:1 провалив би 11px → текст на --text-secondary (#475569 = 5.98:1 AA), перевірено a11y MCP. Header/proof/AnchoredTooltip тур недоторкані. Скоуп розширено founder (інфо-блок→весь hero), напрям через impeccable craft. Урок: «тихий» текст у Frost ≠ muted-foreground для дрібного шрифту — юзай --text-secondary.
Нотатки 2026-06-28 (M-REV-05, commit 8aac403e, founder QA «вогонь»): статистика по типах ціноутворення. Частина 1: огляд-блок «Результати правил» на вкладці Смарт-ціни (PricingRulesOverview, після hero) — 4 правила ранжовані (Пік +₴·N× / знижки N слотів, 0→сіре), тап→наявна PricingRuleStatsSheet. RPC get_pricing_rules_overview (auth.uid без IDOR), блок ховається без даних. Частина 2 (фікс наявного): get_dynamic_pricing_uplift переписано — матч по ТИПУ (прибрано фрагментацію по повному лейблу + мертвий мапінг rule==='peak', markup-only фільтр), +saved_slots; віджет DynamicPricingUplift показує надбавку ₴ І врятовані слоти + чисті назви; проведено через useAnalyticsExtras→AnalyticsPage→BentoSecondary. Міграції 20260628000004+05. Анти-сикофанство: звузив скоуп до реальної прогалини замість редунданту, дорогою знайшов+пофіксив прихований баг аналітики.
Нотатки 2026-06-28 (M-REV-04, commit c0c9020a, очікує візуального QA founder): смарт-ціни преміальний редизайн (hero «Ціни що працюють без тебе» + 2 семантичні секції warm/cool, side-stripe border-l-4 прибрано=бан impeccable, легасі-хекси→Frost-токени, PricingUpgradeGate 3 view токенізовано) + 4 follow-up founder: (A) тап-тултіпи інфо-чіпів (карет-поповер замість мертвого title=), (B) врятовані слоти в hero (read-side getDynamicPricingSavedSlots — закрило діру грилу для discount-only майстра де earned=0), (C) прев'ю по типу правила на рівень секцій, (D) модалка стати по правилу (RPC get_pricing_rule_stats через auth.uid БЕЗ IDOR + BarChart3 sibling-button + PricingRuleStatsSheet: кількість/₴/сер.%/дата/останні). Міграція 20260628000003. Пре-код ритуал brainstorming→craft→grill відпрацював (грил зловив markup-only нюанс ДО коду).
Нотатки 2026-06-28 (M-REV-02 + M-REV-03, commit 255bbcf3 — спільний changeset, очікує візуального QA founder):
- M-REV-02 авто-flash A+B+C + bug тогл. A: обидва виклики RPC → м'який 1-арг, 3-арг строгий оверлоид ДРОПНУТО (20260628000001) — кінець триразового рецидиву сигнатури; ініціатор скасування виключений. B (інверсія дефолту founder): клієнт скасовує → авто-flash ОБОВ'ЯЗКОВО (after()), майстер → confirm-шторка «Слот звільнився». Архітектура: 3 шляхи скасування майстром (BookingCard/Dropdown/DetailsModal) → zustand flashOnCancelStore → ОДНА глобальна FlashOnCancelConfirmSheet у DashboardLayout (per-card шторка демонтувалась разом зі скасованою карткою = корінь «промту не було»). C: notify+flash у after(). BUG тогл не зберігався: MasterContext+layout select без auto_flash_* колонок → useEffect скидав false; +колонки в обидва select +тип. Урок: явний select-список ламає фічу тихо, коли додаєш колонку в БД але не в select; diagnose з БД, не Vercel.
- M-REV-03 статистика акцій. Дзеркало розсилок (broadcast_recipients). Нова flash_deal_recipients + RLS, пишеться при відправці спільним notifyAndRecordFlashDeal (прибрав дубль notify ручний/авто). Тип ручна/авто = reuse booking_id (авто пише id звільненого запису). getFlashDealStats + FlashDealDetailSheet (тип/claimed/список по каналах). Per-channel прапорці з наявності push-підписки/telegram (флеш шле bulk-push). Чесний нюанс: ~6 старих акцій без даних → порожній стан. Урок (вкотре): звіряй живу схему перед обіцянками — взяв перевірений патерн розсилок замість нової вигадки.
Нотатки 2026-06-28 (M-REV-01 + BUGFIX):
- M-REV-01 закрито (commits 6931549a редизайн + 013095ef хаб-шелл): флеш-акції преміальний редизайн. ПЕРШИЙ прогін нового пре-код ритуалу (brainstorming→impeccable craft→grill-me, доданий у WORKFLOW). FlashDealPage: hero вигоди serif + амбер-іскра, грошовий нудж-виручка, живі таймери (timeUntil(expires_at), 1 інтервал/хв), легасі-хекси→токени, CTA помаранч→slate. Хаб-шелл RevenueHubClient (розширення founder): шапку понижено (де-твін з hero), tab-overflow фікс (текст-онлі моб + nowrap + іконки hidden lg). a11y: таймер text-foreground (амбер тінт <4.5).
- BUGFIX 7b6375f8 (founder: «чому 0 клієнтів?»): get_eligible_flash_deal_clients виклик мав НЕІСНУЮЧУ сигнатуру (p_master_id,p_slot_timestamp) — у БД лише 1-арг і 3-арг. Помилка проковтувалась (.rpc без перевірки error) → 0 нотифікацій завжди (ручні+авто). РЕЦИДИВ (вже фіксили в bb9dac0e). Фікс: +p_service_id + error-лог + міграція 20260628000000 (3-арг повертає client_name). 0→3 клієнти на БД. Урок: .rpc() без перевірки error = тихий збій; vercel logs не покаже — джерело дає БД.
Нотатки 2026-06-27 (M-SHOP-03b):
- M-SHOP-03b закрито (commit 9f97b5a5, deploy READY): відгуки про товари на сторінці товару. КОНТРТЕЗА до спеки (Фаза C була невірна): нова таблиця НЕ потрібна — reviews уже має order_id+product_id (міграція 112 уніфікувала), submitReview уже збирає order-відгуки (is_published=false модерація), MyBookingsPage/ShopOrderCard уже має UI. Бракувало лише READ. Реалізовано: RPC get_product_reviews(product_id) derive reviews.order_id→order_items.product_id (як get_service_reviews; мультитовар→під кожним товаром), SECURITY DEFINER hardened, індекси були, smoke ✓, міграція 20260627000010. Хук useProductReviews. ProductDetailView: заглушку→живий блок. Рішення founder: derive (не per-product product_id) + майстер модерує. Урок (утретє): звіряй живу БД перед плануванням backend — він уже існує.
Нотатки 2026-06-27 (M-SHOP-03 A+B):
- M-SHOP-03 (A+B) закрито (commit 19bd7894, deploy READY, очікує візуального QA): сторінка товару = окремий роут /[slug]/shop/[productId] (SSR+OG). Кошик піднято: ShopCartContext (context+localStorage per slug, hydration-safe) у shop/layout.tsx → переживає навігацію каталог↔товар + reload. ShopCartBar (sticky+CartDrawer checkout+OrderSuccess fixed-overlay) винесено зі ShopPage, читає контекст, рендериться на обох сторінках (активна одна). ProductDetailView — спільний presentational (галерея+опис+порожня секція відгуків), юзає публічна сторінка (з cart-actions слотом) і майстер Eye-прев'ю (mode=master read-only Sheet). ShopPage: тайл→Link, ProductDetailSheet видалено. ProductCard майстра: Eye-кнопка (грід-cols-1 на мобілці → 4 footer-кнопки вміщаються). Розбито founder: відгуки (C) → M-SHOP-03b. Уроки: Sheet→роут тягне підняття ефемерного стану (кошик) у persistent (layout-context+localStorage); reviews table намертво прив'язана до bookings → відгуки товару = окрема таблиця product_reviews.
Нотатки 2026-06-27 (M-SHOP-02):
- M-SHOP-02 закрито (commit 4d428d28, deploy READY, очікує візуального QA): картки товарів маркетплейс + 2 режими (сітка/список, перемикач у сайдбарі + localStorage products_view). ProductCard переписано 1:1 з M-SVC-02. Сітка = плитка фото-зверху aspect-[16/10] (Frost icon-fallback) + glass-піл залишку оверлеєм top-right (текст-колір success/warning/destructive); список = горизонт. рядок назва на всю ширину + правий стовпчик ціна-над-діями. Тап по тілу → редактор (рішення founder), аналітика винесена в окрему кнопку BarChart3 (повнокарткову z-0 підкладку прибрано). Спільні actions+toggle, DnD працює в обох режимах (Droppable на grid). Бекенд/хуки/RPC/розхідники не чіпані. Уроки: marketplace-картка товару = клон ServiceCard + 1 поправка (залишок glass-піл на фото, бо full-bg піл нечитабельний); stats-on-tap→edit-on-tap коли є і редактор, і аналітика — тіло на найчастішу дію, вторинне явна кнопка.
Нотатки 2026-06-27 (M-SHOP-01 + Аудит):
- M-SHOP-01 закрито (commit 641141d3): аналітика товару рахує ОБИДВА канали (order_items shop + booking_products на записі, status!=cancelled). getProductStats → soldQty/revenue/profit/marginPct/lastSaleAt. Блок у ProductEditor + overlay Sheet з картки (a11y sibling-button підкладка z-0, контроли z-10). Спільний ProductStatsPanel.
- У ТОМУ Ж КОМІТІ — повний аудит товарів/розхідників (UX→БД), P1 5/5 + P2 4/5: [P1] витік собівартості (колонкові GRANT anon — пасивного витоку не було, vulnerable вектор = anon-ключ напряму), порожня історія складу (product_transactions RLS без політики → pt_master_select), restock booking_products при скасуванні (cancelBooking+updateBookingStatus, дзеркало decrement→increment), idempotency completeBooking (гард status='completed'). [P2] atomic RPC deduct_consumable_stock (заміна read-modify-write), тип 'deduction' замість 'sale' для розхідників, drop permissive INSERT orders/order_items (форжинг), emoji→Info. Міграції 20260627000001-05 (MCP+локально). 13 тестів. Відкладено: P2#9 vaul ShopPage (ризик публічного checkout) + P3 advisors. Звіт: ~/.claude/plans/tranquil-plotting-feather.md.
- Уроки: товар = 2 канали продажів (рахуй обидва); RLS row-only → колонковий захист REVOKE+GRANT(cols) лише для anon (майстер теж authenticated); RLS enabled без політики = тиха поломка читання; списання складу = атомарний RPC + ledger=фактично списане; скасування дзеркалить створення.
Нотатки 2026-06-26 (M-SVC-03):
- M-SVC-03 закрито (commit e2973465): детальна «картка товару» опис+відгуки. БД-урок: reviews не має service_id, createBooking НЕ пише bookings.service_id → per-service відгуки лише через RPC get_service_reviews (reviews.booking_id → booking_services.service_id), SECURITY DEFINER + is_published + grant anon/authenticated. Мультипослуговий запис: відгук візиту під кожною послугою (рішення founder). Клієнт: акцентна «Детальніше» (тап=вибір лишається) → ServiceDetailSheet (темний hero serif-назва поверх = єдиний контраст фото+icon-fallback, ціна фокус, рейтинг+відгуки). Майстер: Eye-прев'ю в grid/list → той самий Sheet mode=master + нудж порожнього опису. impeccable bolder+polish, контраст AA, security-review clean. Очікує візуального QA founder.
Нотатки 2026-06-26 (M-SVC-02):
- M-SVC-02 закрито (commit 980b5402): картки маркетплейс + 2 режими перегляду (сітка/список, перемикач у сайдбарі + localStorage services_view). Сітка = вертик. плитка фото-зверху aspect-[16/10] (Frost icon-fallback) + footer-дії; список = горизонт. рядок, назва на всю ширину (line-clamp-2, без скорочень), правий стовпчик ціна-над-діями. ServiceCard отримав view проп + спільні editDelete/toggle блоки. 3 ітерації founder (гібрид→горизонт→вертикаль + ad-hoc другий режим). Бейдж «Хіт» на популярних. Бекенд/DnD/поля не чіпані. Урок: повні назви — ціна не має бути inline-сусідом імені в горизонт. рядку, винось у окремий стовпчик.
Нотатки 2026-06-26 (M-BOOK-05):
- M-BOOK-05 закрито (commit 0ebd850b, очікує візуального QA): деталь запису лишилась adaptive Sheet (не route — той самий патерн ClientDetailSheet). Receipt-картка (hero serif-дата + час tabular + source-чіп → пунктир → рядки → «Разом» serif 3xl) + новий status-outcome блок для термінальних (status_changed_at + cancellation_reason — раніше мертві поля хука) + термінальні дії «Записати знову» (UrlActionBus booking:create+clientId) / «Профіль клієнта». a11y: пастельний статус-колір як bold-текст провалив контраст (<4.5) → text-foreground, колір лишився на іконці. Урок: звіряй що хук віддає vs що екран показує.
Нотатки 2026-06-26 (M-BOOK-03+04):
- M-BOOK-03+04 закрито (commit 757bcb89, deploy READY): 4 верхні bookings-віджети → клікабельні кнопки + adaptive Sheet з розбивкою; усі елементи overlay ведуть на main-елемент (клієнт→/dashboard/clients?clientPhone, запис→?bookingId — router-навігація, не інлайн-сіти). Нуль нових запитів (DashboardWidgets рахує з bookings; хук +totalBookedMinutes/totalWorkingMinutes). M-BOOK-04: кнопка «Новий запис» a11y (aria-label+текст) + компактний pill ~70/30. Урок: «все клікабельне→main-елемент» = router на канонічну сторінку сутності; «зроби справжню кнопку» ≠ завжди div→button (перевір, чи вже button; вада могла бути a11y/розмір).
Нотатки 2026-06-26 (M-BOOK-02):
- M-BOOK-02 закрито (commit 811482da, deploy READY, 3 ітерації Sonnet→Opus): bolder таймлайн = спец-блок TimelineBlock (статус-рейка + твердий часовий каркас + герой now-line) у VerticalTimeline.tsx + Smart Design System (наповнення адаптується під висоту блока: sm 1 рядок / md-lg top-anchored час-на-лінії / xl 1год+ повна rich-картка justify-between з тривалістю + ціна-футер). Бокові години узгоджено зі шрифтом часу на картках (serif→sans tabular). Тіла пастельні (M-BOOK-01 не відкочено). a11y: статус=рейка+слово(secondary), не лише колір. Урок: Smart Design System на таймлайні = наповнення за висотою; top-anchor проти «плаваючого центру».
Нотатки 2026-06-25:
- M-CLI-06 закрито (commit 1f05146a, deploy READY): глибокий редизайн профілю клієнта (профіль-картка). ClientDetailSheet виявився СПІЛЬНИМ (6 точок виклику) → редизайн покрив клієнтів+дашборд+аналітику; BookingDetailsModal-дубль підтягнуто екстракцією. НОВІ спільні ClientIdentityHeader + ClientStatChips. Реальний LTV (total_spent + ранг + каденс, без міграції). Реальні мітки: міграція vibe_tags text[] (на прод) + saveClientTags + useClientTags. Encoding: 30 латинських i виправлено. Урок: SYSTEM_MAP брехав про tags[] — звіряй схему через live-DB.
- M-BOOK-01 закрито ПОЗА ЧЕРГОЮ (commit 7777a7dc, за гарячим слідом M-CLI-05): пастельний glow на BookingCard, рамка/фіолет-hover геть. Формула glow винесена у спільний lib/utils/statusGlow.ts (retentionGlow делегує). «Підтвердити» лишилась primary (головний CTA). Потребує візуального QA founder.
- M-CLI-05 закрито (commit fa34fb9d): кольорова корекція карток (grid+list) — пастельний radial-glow у кольорі статусу (новий retentionGlow() у clientsUtils), фіолет геть з тіла, accent лише на CTA. Потребує візуального QA founder (сила glow ~8%). Урок: інлайн style перекривав рідну м'яку тінь bento-card — фікс = прибрати перекриття.
- M-CLI-04 закрито перевіркою (без коду): scroll-UX статусів/тегів вже покрито G-PWA-02 — ClientsPage.tsx обгортає retention-чіпи + сегменти в ScrollStrip. Як і M-DASH-03.
- M-CLI-03 закрито (commit 10038f6b + hotfix e954f909): інфо-меседжі dismiss 12год, новий хук useDismissable. HOTFIX: краш хуків на мобілці (early return перед хуками) — урок: early return ТІЛЬКИ після всіх use*.
- M-CLI-02 закрито (commit 72a92ac1): віджет «Важливі/Амбасадори» — REDIRECT founder: картка статична + горизонтальні індикатори знизу.
- M-CLI-01 закрито (commit 94515808): grid-картки єдиний лейаут — h-full + flex-1 + mt-auto.

Беклог = 3 зони: A. Майстер (57) · B. Клієнт (10) · C. Глобальне (7).
Порядок виконання = фази (P0 баги → глобальні основи → майстер → клієнт → лендинг).
Деталі поточної задачі: XDEV/PLANS/SPRINT-05-BACKLOG/HANDOFF.md

═══ СКІЛ-СТРАТЕГІЯ ═══
Універсальні гейти (кожна задача): grilling → робота → impeccable (UI) /
  code-review (код) → security-review (auth/RLS/payments) → humanizer (copy) → ship-gate.
Спеціаліст-скіли на задачу — у колонці TRACKER/BACKLOG.
Джерело каталогу: XDEV/SKILLS_REFERENCE.md

═══ TASK GATE (обов'язково перед кодом) ═══
1. Read HANDOFF.md: деталі поточної задачі (▶ NEXT)
2. mempalace_search по темі задачі
3. QA Gate: задати 3-5 уточнювальних питань
4. Оголосити SKILL: [назва] → одразу запустити Skill tool (ZERO TOLERANCE)
5. UI рядки → /humanizer (якщо є нові)
6. Отримати ОК від юзера → тоді код

═══ ПІСЛЯ КОДУ ═══
□ npx tsc --noEmit (нуль помилок)
□ npm run build (clean)
□ vercel --prod
□ TRACKER.md: [ID] ⬜→✅, вписати commit hash + оновити ▶ NEXT
□ HANDOFF.md: додати секцію [ID] з деталями + root cause; розписати наступну ▶ NEXT
□ TRANSITION_PROMPT.md: оновити "Наступна" → [ID наступної]
□ mempalace_add_drawer
□ Повідомити юзера → він QA → підтверджує → наступна задача

═══ КОНТЕКСТ ═══
Root: C:\Users\Vitossik\SaaS\bookit\
Тема: Frost (єдина; Blossom/Studio = wip)
Stack: Next.js 16, TS strict, Tailwind v4, Supabase, Vaul
Беклог: XDEV/PLANS/SPRINT-05-BACKLOG/BACKLOG.md
Трекер: XDEV/PLANS/SPRINT-05-BACKLOG/TRACKER.md
```

---
