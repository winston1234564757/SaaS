# TRANSITION PROMPT — OPTIMIZATION-BACKLOG

> Скопіюй у нову сесію після `STARTUP OK`.

---

## Контекст

Працюємо за окремим беклогом оптимізації: `XDEV/PLANS/OPTIMIZATION-BACKLOG/`.
Порядок читання: `HANDOFF.md` (стан, блокери, own-eyes рецепт, пастки, знайдені баги) → `TRACKER.md` → бриф конкретної задачі в `BRIEFS/`.

**Стан:** 9/19 ✅ · 1 🔄 (`RND-04` — ч.1 admin зроблено, ч.2 чат лишилась) · 2 ↩️ (`ASSET-03`, `RND-06` — передумови спростовані живим кодом).

---

## ⚠️ Спершу — два пункти на founder

1. **🔴 Apply двох міграцій `ASSET-02b` на прод.** `20260710000000` (розміри вкладень) + `20260710000001` (фікс storage-політик). Застосовані **лише локально**.
   Друга лагодить **пре-існуючий продакшн-баг: аплоад вкладень у DM-чаті ніколи не працював** (деталі — `HANDOFF.md §4c`). Поки міграції не на проді, **вкладення в чаті на бойовому середовищі зламані**. Це блокер деплою чату.
   Apply робить founder: harness блокує прод-write агента. SQL застосовується через Management API, не `db push` (`AUDIT/REPO_PARITY.md`).

2. **17 коммітів не запушено** — свідоме рішення founder (2026-07-11). Весь беклог живе локально на `main`. Точний список: `git log origin/main..HEAD --oneline` (від `7d66e4c4` включно).

---

## Точка старту (кінець сесії 2026-07-11)

Baseline середовища — щоб не робити археологію:

- **Робоче дерево чисте по коду.** Передіснуючий шум, який НЕ чіпати: `.claude/settings.local.json`, `.claude/hooks/state/startup_ok`, `bookit/playwright/.auth/setup-fail-client.png`, `image.png`.
- **`.next` зібрано під ПРОД** (`allowLocalIP=false`). Для e2e — перебудувати через **`npm run build:e2e`**.
- **Порти 3000/3001 вільні**, фонових dev-серверів нема.
- **Docker Desktop не запущений**, локальний Supabase погашений. Піднімати за рецептом `HANDOFF §Own-eyes рig`.
- **`.env.production.local` перейменовано → `.env.e2e-build.bak`** (`0098b30e`). **Не повертати цю назву** — саме вона тихо ламала прод-білд.
- **MemPalace drawer'и:** `bookit/problems` (DM-аплоад, .env) · `bookit/technical` (imageMeta+CHECK-пастка; `drawer_bookit_technical_6d44569f` — віртуалізація RND-04) · `bookit/fixes` (ASSET-02 SSRF) · `bookit/decisions` (RND-06 ↩️).

---

## Наступна задача: обери одну

### A. `OPT-DB-08` — кластер N+1/waterfall (P2)
Фронт + запити, **без DDL** → можна закрити повністю, без founder-apply.
Loyalty per-master RPC · broadcast nested-await · 3 sequential-then-await хуки.
⚠️ Loyalty-частина може потягнути новий RPC = міграцію = знову хвіст під founder-apply. **Перевір це живим кодом ПЕРШИМ ділом** — якщо так, звузь задачу до клієнтських waterfall'ів (їх фіксиш повністю сам).

### B. `OPT-RND-04` ч.2 — віртуалізація `ChatMessageList` (P1)
**ч.1 (`MastersDirectory`) зроблено** — `34864d2c`, spacer-row + `useWindowVirtualizer`.
Чат — **ризиковий**: reverse-scroll, stick-to-bottom, групування (`prev/next/isNewDay` рахується inline у map → винести в мемо-derived масив), конфлікт з `AnimatePresence popLayout`.
Розглянути **`content-visibility: auto`** як низькоризикову альтернативу повній віртуалізації.

### Фаза 2 (DB-міграції) — `OPT-DB-01/02/03/04/05`
Пишу міграції + код; **apply на прод робить founder**. Для `OPT-DB-03` рішення вже прийняте: **варіант A — lazy per-tab**.

---

## Правило цієї роботи

Аудит писався зі **статичного читання** і **п'ять разів** виявився хибним або перебільшеним: `ASSET-03` ↩️ · `DB-07` 5→2 · `RND-05` 6→3 · `ASSET-02` 9→5 · `RND-06` ↩️.

**Перед виконанням будь-якої задачі — перевір її передумову живим кодом.** Не тримається — скасуй або звузь задачу і зафіксуй чому. Це головний урок беклогу.

**Own-eyes = доказ, не припущення.** Сід не має зображень і має застарілий `E2E_CONVERSATION_ID` — перевірка може «пройти», нічого не довівши.

---

## Пастки віртуалізації (з `RND-04` ч.1 — читати перед ч.2)

1. **Нативну `<table>` не віртуалізувати абсолютним `translateY`.** Це вимагає `display:block` на рядках і **ламає auto-width колонок**. Треба **spacer-row**: рядки лишаються в потоці, вікно зверху/знизу добивається порожніми `<tr>` зі spacer-висотою.

2. **`scrollMargin` міряти від початку віртуалізованих рядків**, а не від контейнера над ними (у таблиці — від `<tbody>`, не `<table>`; інакше постійний зсув на висоту `<thead>`).

3. **🔴 `useLayoutEffect` на mount НЕ працює, якщо контейнер за `loading`-гейтом.** На момент ефекту вузла ще нема в DOM → `scrollMargin` лишиться `0` **назавжди** (нічого не тригерить перерахунок, коли дані прийшли) → позиціювання зламане. Потрібен **callback-ref** зі стабільною identity, який форсить ререндер на монтуванні (і перемонтуванні — фільтр дав 0 рядків і назад).
   `ClientsPage` цієї проблеми не має **лише** тому, що його контейнер рендериться завжди.
   **Ширший урок: не копіювати референс-патерн наосліп у компонент з іншими умовами монтування.**

---

## Пастки рігу (перед підняттям own-eyes)

- Перед e2e — **`npm run build:e2e`**, інакше `npm run start` на прод-білді піде в **ПРОД-БД**.
- **`21-direct-messages` падає і на чистому `main`** (застарілий `E2E_CONVERSATION_ID`; `test:e2e:seed` падає на `booking_slot_collision` і не переписує runtime-файл). **Не приймати за регресію.**
- Порт 3000 може тримати чужий dev — він **відповість 200**, а спека битиме не в твій сервер. Перевіряти власником: `Get-NetTCPConnection -LocalPort 3000 -State Listen` → `Get-Process`.
- Пайп (`| head`) **вбиває** фоновий `npm run dev`.
- `next/image` + локальний Supabase: Next 16 SSRF-гард відхиляє `127.0.0.1` **після** матчу патерна і кидає **той самий текст** помилки. Потрібні **обидва** гейти: локальний `remotePattern` **і** `dangerouslyAllowLocalIP`.
- `storage.objects` не чистити прямим `DELETE` (тригер `protect_delete`) — лише через Storage API.
- Тимчасові спеки класти в `e2e/tests/` — `e2e/inspect/` не бачиться (`testDir`).

---

## Не перепитувати (рішення прийняті)

`Sheet blur` = варіант C · `Analytics DB-03` = варіант A (lazy per-tab) · `--color-error` = окрема задача, **не** в цьому беклозі · `OPT-EXPL-01` = ⏸ до after-launch, **не форсити перед лончем** · Чат-вкладення = зроблено якісно, розміри в БД · `RND-04` = **розбито надвоє**, admin окремо від чату · Пуш = **лишаємо локально**, поки founder не скаже інакше.

---

## Борг верифікації (чесно)

`RND-04` ч.1 верифіковано **неповно**: `tsc` 0 + `build` clean + рев'ю діфа. **Живу scroll-поведінку ніхто не спостерігав** — founder мав глянути сам у dev на admin-каталозі майстрів (рядки не накладаються, скрол плавний, пошук/фільтр тарифу миттєво перемальовує список).
Якщо founder цього ще не зробив — **спитати перед тим, як будувати щось поверх**.
