# TRANSITION PROMPT — OPTIMIZATION-BACKLOG

> Скопіюй у нову сесію після `STARTUP OK`.

---

## Контекст

Працюємо за окремим беклогом оптимізації: `XDEV/PLANS/OPTIMIZATION-BACKLOG/`.
Прочитай спершу: `HANDOFF.md` (стан, блокери, own-eyes рецепт, пастки, знайдені баги) → `TRACKER.md` → бриф конкретної задачі в `BRIEFS/`.

**Стан:** 9/19 ✅ · 1 ↩️ · коміти на `main`, **не запушено**.

---

## ⚠️ Спершу — два пункти на founder

1. **Apply двох міграцій `ASSET-02b` на прод** (`20260710000000` розміри вкладень, `20260710000001` фікс storage-політик). Застосовані лише локально. Друга лагодить **пре-існуючий баг: аплоад вкладень у DM-чаті ніколи не працював** — деталі в `HANDOFF.md §4c`.
2. Запушити коміти або свідомо лишити локально.

---

## Наступна задача: обери одну

### A. `OPT-RND-06` — акордеони `height:0→auto` × 5
Чистий фронт, без міграцій, не блокується. Перед виконанням — **перевір передумову живим кодом** (`fixing-motion-performance` дозволяє поодинокі entrance-анімації; `RND-05` через це звузився 6→3).

### B. `OPT-DB-08` — кластер N+1/waterfall
Фронт + запити, без DDL. Loyalty per-master RPC, broadcast nested-await, 3 sequential-then-await хуки.

### C. `OPT-RND-04` — віртуалізація
Розбити: `MastersDirectory` (admin, простіше) окремо від `ChatMessageList` (ризиково: reverse-scroll, stick-to-bottom, `popLayout`). Для чату розглянути `content-visibility: auto` як низькоризикову альтернативу.

### Фаза 2 (DB-міграції) — `OPT-DB-01/02/03/04/05`
Пишу міграції + код; **apply на прод робить founder**. Для `OPT-DB-03` рішення вже прийняте: **варіант A — lazy per-tab**.

---

## Правило цієї роботи

Аудит писався зі статичного читання і **чотири рази** виявився хибним або перебільшеним (ASSET-03 скасовано, DB-07 звужено 5→2, RND-05 звужено 6→3, ASSET-02 звужено 9→5).

**Перед виконанням будь-якої задачі — перевір її передумову живим кодом.** Якщо передумова не тримається, скасуй або звузь задачу і зафіксуй чому.

**Own-eyes = доказ, не припущення.** Сід не має зображень і має застарілий `E2E_CONVERSATION_ID` — перевірка може «пройти», нічого не довівши. Читай §Пастки в `HANDOFF.md` перед підняттям рігу. Найважливіше:

- перед e2e — **`npm run build:e2e`**, інакше `npm run start` піде в **ПРОД-БД**;
- `21-direct-messages` падає **і на чистому `main`** — не приймати за регресію;
- порт 3000 може тримати чужий dev (відповість 200); пайп (`| head`) вбиває фоновий `npm run dev`.

---

## Не перепитувати (рішення прийняті)

Sheet blur = варіант C · Analytics DB-03 = варіант A (lazy per-tab) · `--color-error` = окрема задача, не в цьому беклозі · `OPT-EXPL-01` = ⏸ до after-launch, не форсити · Чат-вкладення = зроблено якісно, розміри в БД.
