# TRANSITION PROMPT — OPTIMIZATION-BACKLOG

> Скопіюй у нову сесію після `STARTUP OK`. Стан на кінець сесії **2026-07-12**.

---

## Контекст

Працюємо за окремим беклогом оптимізації: `XDEV/PLANS/OPTIMIZATION-BACKLOG/`.
Порядок читання: `HANDOFF.md` (стан, пастки, own-eyes рецепт, знайдені баги) → `TRACKER.md` → бриф конкретної задачі в `BRIEFS/`.

**Стан беклогу:** 11/19 ✅ · 0 🔄 · 3 ↩️ (`ASSET-03`, `RND-06`, `RND-04` ч.2 — передумови спростовані живими даними) · 1 ⏸ (`EXPL-01`, after-launch).

**Лишилась одна фаза: ФАЗА DB — `OPT-DB-01/02/03/04/05`.** Боргів і блокерів під founder нема.

---

## ✅ Боргів нема — ні по БД, ні по пам'яті

**Прод-БД і репо синхронні.** `db push --dry-run` → `Remote database is up to date` (exit 0).
`migration list --linked`: **185 спарено · 0 local-only · 0 orphans**. `db push` більше не міна —
але деплой-флоу лишається `vercel --prod` + Management API.
Повний розбір: `XDEV/AUDIT/REPO_PARITY.md §HALF #2 ЗАКРИТО`.

**Борг пам'яті закрито 2026-07-12.** Три drawer'и, які не записались через падіння MCP-хендлів,
перенесено в палац: `bookit/fixes` (фікс «Отримано») + `bookit/technical` ×2 (реєстр міграцій + 2 діри;
`mood_theme` + `137a`). Плюс diary-запис.

**Застосовано на прод** (усе перевірено запитами ПІСЛЯ застосування, не з відповіді API):

| Міграція | Що дала |
|---|---|
| `20260710000000` + `20260710000001` (ASSET-02b) | розміри вкладень + **фікс DM-аплоаду**. Працює вже зі старим задеплоєним кодом — баг був у політиках БД, а не в коді |
| `20260710000002` lock_stock_rpcs | 🔴 **закрито діру:** stock-RPC були відкриті **anon** → можна було обнулити склад майстра через REST |
| `20260604000000` booking_link_security | 🔴 **закрито діру:** таблиці `link_attempts` не існувало → rate-limit «5 спроб/15 хв» на прив'язку бронювання **не працював узагалі** |
| `20260712000000` mood_theme_default_frost | DEFAULT `'default'` → `'frost'`. Винен був **Google OAuth** (`auth/callback` upsert-ить профіль без теми), а не реєстрація. Прод: 11/11 майстрів на frost |

**Також:** `137a_product_type_and_emoji.sql` видалено (CLI скіпав його **завжди** — літера в префіксі; зміст перекрито `139`).

### 🔴 Три уроки, які не можна забути

1. **«unregistered ≠ not applied».** Теза старого аудиту («незареєстровані міграції насправді застосовані, просто не записані») — **хибна**. По-об'єктна звірка знайшла дві реально відсутні (таблиця вище). **Ніколи не роби `migration repair --status applied` наосліп:** позначиш застосованим те, чого нема → воно зникне з-під `db push` назавжди, а діра лишиться відкритою.
2. **Одноразовий `UPDATE` у міграції нічого не варт, якщо не виправлено ДЖЕРЕЛО** (DEFAULT колонки або код вставки). Симптом, за яким це ловиться: рядки з «неправильним» значенням, створені **пізніше** за саму міграцію. Саме так знайшовся Google-шлях у `mood_theme`.
3. **🆕 Задачу, обґрунтовану ОБСЯГОМ, спершу ЗАМІРЯЙ на живих даних.** Читання коду каже, що станеться при великому N. Воно нічого не каже про те, **чи існує велике N**. `RND-04` ч.2 закрився не аналізом коду, а одним `count(*)` на проді.

**Комміти НЕ запушено** — свідоме рішення founder. Весь беклог живе локально на `main`: `git log origin/main..HEAD --oneline`.

---

## Точка старту (baseline середовища)

- **Робоче дерево чисте по коду.** Передіснуючий шум, який НЕ чіпати: `.claude/settings.local.json`, `.claude/hooks/state/startup_ok`, `bookit/playwright/.auth/setup-fail-client.png`, `image.png`.
- **`.next` зібрано під ПРОД.** Для e2e — перебудувати через **`npm run build:e2e`**.
- **Порти 3000/3001 вільні**, фонових dev-серверів нема.
- **Docker Desktop ЗАПУЩЕНИЙ, локальний Supabase ПІДНЯТИЙ.** Гасити: `npx supabase stop`. Сід на місці; усе, що сіялось під перевірки (c2c, broadcast), **прибрано** — лічильники в 0. Локально застосовано `20260712000000`.
- **🔴 Годинник Docker/WSL дрейфує** після сну хоста → GoTrue кидає `JWT issued at future`, і будь-який `signInWithPassword`/e2e падає з цим текстом. Виглядає як проблема авторизації, але це **час**. Лікується `wsl -d docker-desktop -e /sbin/hwclock -s`.
- **`.env.production.local` перейменовано → `.env.e2e-build.bak`** (`0098b30e`). **Не повертати цю назву** — саме вона тихо ламала прод-білд.

### Прод-apply — перевірений рецепт

Management API `/database/query`, токен `SUPABASE_ACCESS_TOKEN` у `.env.local`, ref `sqlrxsopllgztvgrerqk`.
Один файл за виклик = неявна транзакція (все-або-нічого). Після apply — **обов'язково вписати version у `supabase_migrations.schema_migrations`**, інакше реєстр знову поїде.
Той самий ендпоінт read-only годиться для **замірів** (`count(*)`, `max(...)`) — користуйся ним перед кожною DB-задачею.

- ⚠️ **PowerShell 5.1 псує тіло запиту:** `Get-Content -Raw | ConvertTo-Json` дає `{"query":{"value":…}}` → API кидає `Expected string, received object`. Треба `[IO.File]::ReadAllText()` + `[pscustomobject]`.
- ⚠️ `supabase migration list` через PS-пайп ламається переносами рядків → знімати через `cmd /c "… > file 2>&1"`.
- ⚠️ **Перевіряй унікальність version перед вставкою в реєстр** — два файли вже мали однакову (`20260710000000`), і запис однієї «поховав» би другу.

---

## Наступна задача — ФАЗА DB

> ↩️ `OPT-RND-04` ч.2 (віртуалізація `ChatMessageList`) **скасовано 2026-07-12 за замірами прода**: найдовша DM-розмова = **1 повідомлення**, найдовший тікет = **9**, розмов >100 — нуль. Різати нічого. Знайдені попутно (НЕ полагоджені, свідомо): мертвий `AnimatePresence mode="popLayout"` без жодного `exit` і `layout` на кожному повідомленні (O(N) reflow на кожне нове). **Тригер повернення: перша розмова >150 повідомлень** — і тоді порядок не «віртуалізація», а зняти `layout` → `.limit(N)` + догортати → і лише потім віртуалізація. Деталі: `HANDOFF §OPT-RND-04 ч.2`.

Лишилось п'ять задач, усі в `BRIEFS/`:

| ID | Суть | P |
|---|---|---|
| `OPT-DB-01` | `useOrders` — full-scan orders + УСІХ bookings без date-window/limit | P0 |
| `OPT-DB-02` | `get_master_clients` без пагінації + refetch на кожен keystroke | P0 |
| `OPT-DB-03` | Analytics eager mega-RPC → **варіант A: lazy per-tab** (рішення прийняте) | P0 |
| `OPT-DB-04` | `useDashboardStats` тягне 5000 рядків для set-diff у JS | P1 |
| `OPT-DB-05` | `get_finance_analytics` — 8 окремих скан-проходів | P1 |

**🔴 Два фільтри перед кодом на КОЖНІЙ із них:**

1. **Заміряй обсяг на проді.** Усі п'ять обґрунтовані обсягом. Прод сьогодні — **11 майстрів**; «5000 рядків для set-diff» (`DB-04`) майже напевно перебільшення. Якщо обсягу нема — звужуй або скасовуй, як `RND-04` ч.2.
2. **Перевір, чи міграція взагалі потрібна.** `DB-08` бриф обіцяв новий RPC — виявилось, DDL не потрібен зовсім. `DB-04` — найімовірніший наступний кандидат закритись без DDL.

---

## Правило цієї роботи

Аудит писався зі **статичного читання** і **сім разів** виявився хибним, перебільшеним або надто песимістичним: `ASSET-03` ↩️ · `DB-07` 5→2 · `RND-05` 6→3 · `ASSET-02` 9→5 · `RND-06` ↩️ · `DB-08` (міграція зайва) · **`RND-04` ч.2 ↩️**.

**Перед виконанням будь-якої задачі — перевір її передумову живим кодом І живими даними.** Не тримається — скасуй, звузь або **спрости** задачу і зафіксуй чому. Працює в обидва боки: бриф може перебільшити не лише проблему, а й **ціну фіксу**.

**Own-eyes = доказ, не припущення.** Сід не має ані зображень, ані c2c/broadcast-рядків, і має застарілий `E2E_CONVERSATION_ID` — перевірка може «пройти», нічого не довівши. Сій edge-кейси сам (у DB-08: over-spend для клемпа + pending-реферал + чужий власник для IDOR) і **прибирай за собою**.

**Коли RLS обійдено admin-клієнтом — happy-path тесту НЕ достатньо.** Ownership тримається виключно на прикладному фільтрі → обов'язковий **негативний тест чужим власником**, інакше тихий IDOR.

---

## Пастки віртуалізації (з `RND-04` ч.1 — якщо колись повернемось до ч.2)

1. **Нативну `<table>` не віртуалізувати абсолютним `translateY`.** Це вимагає `display:block` на рядках і **ламає auto-width колонок**. Треба **spacer-row**: рядки лишаються в потоці, вікно зверху/знизу добивається порожніми `<tr>` зі spacer-висотою.
2. **`scrollMargin` міряти від початку віртуалізованих рядків**, а не від контейнера над ними (у таблиці — від `<tbody>`, не `<table>`; інакше постійний зсув на висоту `<thead>`).
3. **🔴 `useLayoutEffect` на mount НЕ працює, якщо контейнер за `loading`-гейтом.** На момент ефекту вузла ще нема в DOM → `scrollMargin` лишиться `0` **назавжди** → позиціювання зламане. Потрібен **callback-ref**, який форсить ререндер на монтуванні (і перемонтуванні — фільтр дав 0 рядків і назад). `ClientsPage` цієї проблеми не має **лише** тому, що його контейнер рендериться завжди.
   **Ширший урок: не копіювати референс-патерн наосліп у компонент з іншими умовами монтування.**
4. **`ChatMessageList` має ВЛАСНИЙ скрол-контейнер** (`overflow-y-auto`), не window-scroll → `useWindowVirtualizer` із ч.1 сюди **не переноситься**, треба `useVirtualizer` з `getScrollElement`.

---

## Пастки рігу (перед підняттям own-eyes)

- Перед e2e — **`npm run build:e2e`**, інакше `npm run start` на прод-білді піде в **ПРОД-БД**.
- **`21-direct-messages` падає і на чистому `main`** (застарілий `E2E_CONVERSATION_ID`; `test:e2e:seed` падає на `booking_slot_collision` і не переписує runtime-файл). **Не приймати за регресію.**
- Порт 3000 може тримати чужий dev — він **відповість 200**, а спека битиме не в твій сервер. Перевіряти власником: `Get-NetTCPConnection -LocalPort 3000 -State Listen` → `Get-Process`.
- Пайп (`| head`, `| Select-Object -First N`) **вбиває** фоновий процес — обірваний стрім гасить `npm run dev`/`npm run build`.
- `next/image` + локальний Supabase: Next 16 SSRF-гард відхиляє `127.0.0.1` **після** матчу патерна і кидає **той самий текст** помилки. Потрібні **обидва** гейти: локальний `remotePattern` **і** `dangerouslyAllowLocalIP`.
- `storage.objects` не чистити прямим `DELETE` (тригер `protect_delete`) — лише через Storage API.
- Тимчасові спеки класти в `e2e/tests/` — `e2e/inspect/` не бачиться (`testDir`).

---

## Не перепитувати (рішення прийняті)

`Sheet blur` = варіант C · `Analytics DB-03` = варіант A (lazy per-tab) · `--color-error` = окрема задача, **не** в цьому беклозі · `OPT-EXPL-01` = ⏸ до after-launch, **не форсити перед лончем** · Чат-вкладення = зроблено якісно, розміри в БД · `RND-04` = ч.1 ✅ admin, **ч.2 ↩️ скасовано** · Пуш = **лишаємо локально**, поки founder не скаже інакше.

---

## Боргів верифікації нема

- `RND-04` ч.1 (`34864d2c`): `tsc` 0 + `build` clean + рев'ю діфа + **жива поведінка перевірена founder'ом у dev**. Агент рігу не піднімав — і в доках це записано саме так, без підміни.
- `OPT-DB-08` (`b5345ece`) + фікс «Отримано» (`7a21a1a5`): `tsc` 0 · прод-`build` clean · паритет із RPC на живій БД **від імені залогіненого клієнта** · негативний IDOR-тест для broadcast · жива сторінка `/my/loyalty` у браузері.
- Прод-міграції (`bdbed662`, `862e278f`, `5dad6eaa`): кожна перевірена **запитами до прода після застосування** (об'єкти, привілеї, RLS, таблиця істинності CHECK). Бекап реєстру: `XDEV/AUDIT/schema_migrations_backup_2026-07-11.json`.
- `RND-04` ч.2 ↩️: коду не писано — рішення стоїть на **замірі прода** (запит у `HANDOFF`, відтворюваний).
