# TRANSITION PROMPT — OPTIMIZATION-BACKLOG

> Скопіюй у нову сесію після `STARTUP OK`.

---

## Контекст

Працюємо за окремим беклогом оптимізації: `XDEV/PLANS/OPTIMIZATION-BACKLOG/`.
Прочитай спершу: `HANDOFF.md` (стан, блокери, own-eyes рецепт, пастки, знайдені баги) → `TRACKER.md` → бриф конкретної задачі в `BRIEFS/`.

**Стан:** 8/18 ✅ · 1 ↩️ · 7 комітів коду на `main`, **не запушено**.

---

## Наступна задача: обери одну

### A. `OPT-ASSET-02b` — вкладення чату → next/image (**рішення founder: робити якісно**)

**Тип:** MOTION + DB · **Пріоритет:** P2 · **Бриф:** `BRIEFS/OPT-ASSET-02b.md`

Зберігати `attachment_width/height` у БД і рендерити `next/image` з реальним аспектом; для старих рядків — fallback на raw `<img>`.

Обсяг більший, ніж здається: **2 таблиці** (`support_messages`, `direct_messages`), **3 actions**, **4 upload-місця**, **2 хуки**, спільний тип `ChatMessage`.
**Тягне міграцію** → apply на прод робить founder.

### B. `OPT-RND-06` — акордеони `height:0→auto` × 5

Чистий фронт, без міграцій, не блокується. Перед виконанням — **перевір передумову живим кодом** (`fixing-motion-performance` дозволяє поодинокі entrance-анімації; `RND-05` через це звузився 6→3).

### C. `OPT-DB-08` — кластер N+1/waterfall

Фронт + запити, без DDL. Loyalty per-master RPC, broadcast nested-await, 3 sequential-then-await хуки.

### Фаза 2 (DB-міграції) — `OPT-DB-01/02/03/04/05`
Пишу міграції + код; **apply на прод робить founder**. Для `OPT-DB-03` рішення вже прийняте: **варіант A — lazy per-tab**.

---

## ⚠️ Спершу віддай founder на рішення

**`.env.production.local` — забутий тимчасовий e2e-оверрайд** (з 2026-07-08, сам себе позначив «Remove after e2e»). Через нього **будь-який локальний `npm run build` збирається проти локального Supabase**, а не проду — CSP і `remotePatterns` беруть локальні значення. Gitignored, тож Vercel-прод чистий, але локальна pre-deploy перевірка нічого не перевіряє. Деталі: `HANDOFF.md §Знайдено попутно #4`.

---

## Правило цієї роботи

Аудит писався зі статичного читання і **чотири рази** виявився хибним або перебільшеним (ASSET-03 скасовано, DB-07 звужено 5→2, RND-05 звужено 6→3, ASSET-02 звужено 9→5).

**Перед виконанням будь-якої задачі — перевір її передумову живим кодом.** Якщо передумова не тримається, скасуй або звузь задачу і зафіксуй чому. Не виконуй беклог наосліп.

**Own-eyes = доказ, не припущення.** Сід не має зображень і не має occupancy — на голому сіді перевірка може показати fallback і «пройти», нічого не довівши. Читай §Пастки в `HANDOFF.md` перед підняттям рігу.

---

## Не перепитувати (рішення прийняті)

Sheet blur = варіант C · Analytics DB-03 = варіант A (lazy per-tab) · `--color-error` = окрема задача, не в цьому беклозі · `OPT-EXPL-01` = ⏸ до after-launch, не форсити · Чат-вкладення = робити якісно, з розмірами в БД (не антипатерн `width={0}`).
